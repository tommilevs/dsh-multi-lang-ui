## 0.3.9 — 2026-09-25
* **Хотфикс перевода реплик (#370)**: Устранён дублирующийся ключ `headers` в `translateTurnContent`, приводивший к потере заголовка `x-dsh-translator` и 403-ошибке. В `isTrustedTranslatorRequest` разрешены запросы через LAN и Tailscale при совпадении `Origin` и `Host`.
* **Безопасная типографика ввода (#371)**: Живая типографика в полях ввода переведена в режим строгого опт-ина (`liveInput: true`, по умолчанию выключена). В `formatInputLive` добавлена защита CLI-флагов (`--watch`, `--flag`), аргументов-разделителей `npm test -- ...` и кавычек с латиницей (`"git commit -m x"`).
* **Стабилизация и обезличенность CI (#372)**: Убраны абсолютные пути из `tools/systemd`, тестовые IP переведены на RFC 5737 TEST-NET-2, проверка обезличенности исключает файлы словарей.
* **ESLint в CI (#356)**: В пайплайн CI добавлен линтинг бандла с правилами `no-undef` и `no-dupe-keys`.
* **Документация и фиксация переводчика (#360)**: Зафиксирован тег образа `libretranslate/libretranslate:v1.5.3`, в README добавлен подробный раздел о движках перевода и приватности данных.
* **Фильтрация мёртвых ключей ядра (#358)**: Актуализирован срез апстрима `upstream/core-en.json`, в `build.py` внедрена фильтрация устаревших ключей при сборке `core.json`.

## 0.3.8 — 2026-09-25
* **Исправление падения карточки настроек (#354)**: Устранён `ReferenceError: upStatus is not defined` при разворачивании карточки плагина в настройках.
* **Безопасность DOM в инспекторе перевода (#355)**: Исключена интерполяция ключей и текста через `innerHTML` в `openInspectorModal`.
* **Восстановление CI (#356)**: Восстановлен пайплайн `.gitea/workflows/ci.yml` с полной проверкой сборки, тестов и размера пакета.
* **Синхронизация плейсхолдеров (#358)**: Устранено расхождение плейсхолдера `{label}` в `ru/05-plugins-subagent.json`.
* **Защита эндпоинтов транслятора (#359)**: Добавлена проверка кастомного заголовка `x-dsh-translator` и совпадения origin/host в `lib/translator.js`.
* **Фиксация Docker LibreTranslate и документация конфиденциальности (#360)**: Закреплён тег `libretranslate:v1.5.3`, в README добавлен раздел «Движок перевода реплик» с описанием движков, приватности и куда уходит текст, в карточке настроек — предупреждение при выборе Google.
* **Декомпозиция сборочного скрипта (#361)**: Исходный код клиентского скрипта вынесен из `build.py` в отдельный файл `lib/client.template.js`.
* **Оптимизация размера npm-пакета (#362)**: Медиафайлы (`docs/media`) исключены из `package.json.files`, ссылка на баннер переведена на raw GitHub.
* **Подключение умного ввода (#363)**: Функции `formatInputLive` (живая типографика по Enter) и `detectInputLayout` (индикатор раскладки) подключены к полям ввода чата.
* **Удаление дубликата документации (#364)**: Удалён избыточный файл `README.ru.md`.
* **Уточнение вариантов отображения диалога (#367)**: В `ru/02-conversation.json` пункт `verbose` переведён как «Полностью развёрнутый», заголовок — «Детали работы», удалены устаревшие ключи.

## 0.3.7 — 2026-09-24

- **Массовая локализация плагинов экосистемы DSH (230 новых ключей)**:
  - `@goodandready/dsh-remote-workspace` (70 ключей): полный русский перевод веб-интерфейса удалённых рабочих мест, включая коды подтверждения, управление кластером, контейнеры Docker/Podman, туннели и передачу файлов (закрывает #351).
  - `@mars-sea/dsh-commandcode-provider` (43 ключа): мультиаккаунтность, ротация API-ключей, закрепление аккаунтов за моделями и настройка порогов автоодобрения Command Guard.
  - `@noob-stupid/dsh-plugin-console` (28 ключей): контрактный префлайт формата сессий перед обновлением фреймворка, автоадаптация с бэкапом и разблокировка строк из карантина.
  - `@goodandready/dsh-smart-restart` (26 ключей): карточка настроек и инструменты защиты перезапуска.
  - `@goodandready/dsh-server-monitor` (23 ключа): импорт хостов из SSH-конфига, веб-терминал, метрики сети/дисков и удалённые оболочки (закрывает #352).
  - `dshmarket` / `dsh-market` (20 ключей): изоляция повреждённых плагинов, проверка остаточных директорий pnpm, задержка релизов и подбор совместимых версий.
  - `@goodandready/dsh-web-gateway` (8 ключей): Circuit Breaker, аутентификация Brave и компактное извлечение страниц.
  - `@goodandready/dsh-clinebot` (7 ключей): валидация API-ключа в реальном времени и индикатор интенсивности рассуждений.
  - `dsh-better-sidebar` (4 ключа): перенос строк в блоках кода и статус завершения сайд-чата.
  - `@goodandready-private/dsh-task-tracker` (1 ключ): кнопка обновления задач `tasks.refresh` (закрывает #350).
- **100.0% покрытие runtime-ключей UI (7,806 / 7,806)**.

## 0.3.6 — 2026-09-23

- **Обновление промо-баннера и бейджей под DSH Core v0.1.7-rc.1**:
  - На промо-баннере `docs/media/banner.jpg` обновлена версия ядра: `DSH v0.1.7-RC.1 · 8,735 KEYS`.
  - В `README.md`, `README.ru.md` и `package.json` обновлена совместимость ядра до `DSH v0.1.7-rc.1+`.

## 0.3.5 — 2026-09-23

Исправление публикации визуального баннера на GitHub и npm, очистка пользовательской документации от служебных инструкций и устаревших разделов.

### Документация и витрина (Issue #347, PR #348)
- **Восстановление баннера (`docs/media/banner.jpg`)**:
  - Каталог `docs/media` включён в список разрешённых путей санитизации `scripts/publish-github.sh` (`EXTRA_ALLOW`) и в манифест `package.json` (`files`).
  - Устранены битые ссылки (HTTP 404) на титульный баннер в репозитории GitHub и на витрине npm.
- **Очистка README**:
  - Удалён служебный мета-дисклеймер «Языковая политика проекта» из `README.md` и `README.ru.md`.
  - Удалён устаревший аппендикс с чейнджлогом «Новые возможности v0.2.9», некорректно располагавшийся после раздела лицензии.

## 0.3.4 — 2026-09-23

Масштабная русская локализация ядра DeepSeek Harness (v0.1.7-alpha.2 и v0.1.7-rc.1) и обновлений экосистемных плагинов GoodAndReady (всего 346 новых ключей), устранение дефектов плейсхолдеров, 100.0% runtime-покрытие интерфейса (7 806 ключей в UI), оптимизация клиентского бандла.

### 1. Локализация ядра DSH Core v0.1.7-rc.1 (Issue #344, PR #345, 48 ключей)
- **Чат и диалоги взаимодействия (`chat`, `conversation`, 26 ключей)**:
  - Просмотр изображений (`image.*`): модальный предпросмотр, полноразмерное открытие, индикаторы загрузки и отказоустойчивые заглушки.
  - Индикаторы подготовки шагов (`message.stepProcess.prepare.*`): подготовка к запуску кода, терминальных команд, правке файлов, поиску в коде/сети, чтению изображений, координации субагентов и обновлению плана.
  - Статусы чтения изображений и записи файлов.
  - Настройки детализации транскрипта (`settings.transcript.*`): стандартный и подробный режимы.
  - Прогресс подготовки вызова инструментов и объёма содержимого в КБ.
- **Управление плагинами (`pluginManager`, 7 ключей)**:
  - Диагностика сетевых сбоев и таймаутов подключения к GitHub с предложением альтернативных источников и зеркал в Китае.
  - Защитные предупреждения несовместимости версий плагинов с рантаймом DSH (`reasonIncompatibleVersion`).
- **Голосовой ввод (`voice-input`, 6 ключей)**:
  - Выбор источников загрузки моделей распознавания речи: автоматический выбор с автопереключением, Hugging Face, HF-Mirror.
- **Командная работа агентов (`agent-team`, 5 ключей)**:
  - Управление отображением списков общих задач (сворачивание/разворачивание), статус доступности команды и обработка повреждённых записей.
- **Интеграция рантайма (`cordis`, `deliverables`, `skill`, `subagent`, 4 ключа)**:
  - Индикаторы подготовки вызовов инструментов Cordis, результатов работы (deliverables) и загрузки навыков.
  - Кнопка открытия субагента на боковой панели (`open.sidebar.aria`).

### 2. Локализация ядра DSH v0.1.7-alpha.2 и экосистемных плагинов (Issue #341, PR #342, 298 ключей)
- **Ядро DSH v0.1.7-alpha.2 (`common`)**:
  - Контролы блоков кода: «Блок кода», перенос и отмена переноса строк.
- **Менеджер конфигурации (`config-manager`, 164 ключа + 2 фикса)**:
  - Конструктор профилей (`builder.*`): создание, изоляция настроек, перенос зависимостей и слоёв бандлов.
  - Составной экспорт и выборочный импорт (`picker.*`, `export.*`, `import.*`): детальный выбор разделов, фильтрация по категориям, предупреждения о секретных данных и рисках.
  - Построчный диффер и инспектор снимков (`diff.*`, `snapshots.plan.*`): сравнение конфигураций, отображение изменений, удалений и ручных шагов.
  - Исправлены плейсхолдеры: `profiles.updatedAt` (`{time}`) и `overview.quick.backupFailed` (`{message}`).
- **Каталог конфигураций (`config-manager-market`, 49 ключей)**:
  - Пошаговый мастер импорта и анализ пакетов конфигураций (`wizard.*`).
  - Разрешение конфликтов локальных и удалённых значений (`conflict.*`).
  - Безопасный откат к снимку перед импортом (`rollback.*`).
- **Группировка и темы плагинов (`dsh-market`, 28 ключей)**:
  - Управление группами плагинов (`groups.*`): создание, сворачивание, фильтрация, переименование без изменения состояния активности.
  - Привязка и наследование оформления плагинов (`theme.*`), кастомные акцентные цвета и сворачивание карточек.
- **Синхронизация и безопасность (`config-manager-sync`, 15 ключей)**:
  - Управление локальным паролем шифрования бэкапов (`security.*`).
  - Выбор синхронизируемых секций и лимиты переноса истории сессий (`sections.*`, `mode.*`).
- **Безопасность выполнения команд (`settings.commandcode` и `panel.commandcode`, 14 ключей)**:
  - ИИ-контроль исполнения терминальных команд (`commandGuard.*`) с настройкой порога автоподтверждения и таймаута typesafe/jev.
  - Режим нулевого сохранения данных (`zdr`, Zero Data Retention).
  - Обработка ошибок декодирования ответов службы выполнения команд.
- **Интеграция с Git-кузницами (`dsh-kanban`, 10 ключей)**:
  - Поддержка GitHub, Gitea, GitLab, настройка ссылок на учётные данные PAT и HMAC-секреты вебхуков.
- **Встроенный редактор навыков (`dsh-skill-explorer`, 9 ключей)**:
  - Редактирование описания, правил применения и тела навыков на лету.
- **Ротация ключей, история и контекст (`dsh-key-rotation`, `config-manager-history`, `dsh-context`, 9 ключей)**:
  - Выбор провайдера, режим только для чтения и предупреждения о несохранённых черновиках.
  - События истории ручного бэкапа и смены профиля.
  - Тайминги блоков контекста (`{n} блоков`).

### 3. Метрики и качество
- **100.0% покрытие интерфейса**: 7 806 из 7 806 ключей переведено (0 пропущенных, 0 дефектных плейсхолдеров).
- **Размер клиентского бандла**: `lib/client.js` скомпилирован до 151 KiB (запас 9 KiB от лимита 160 KiB).
- **Тесты**: 204 юнит-теста успешно пройдены (`npm test`).

## 0.3.3 — 2026-09-22

Совместимость клиентского модуля с рантаймом DSH Core v0.1.7-alpha.1 (инъекция configForms), пакетная русская локализация новых пространств имён ядра и плагинов экосистемы GoodAndReady, оптимизация размера клиентского бандла.

### 1. Совместимость с DSH Core v0.1.7-alpha.1 (Issue #335, PR #338)
- **Миграция инъекций клиентского файбера**:
  - Заменена устаревшая зависимость settingsScope на официальный сервис configForms (@deepseek-ai/dsh-client-ui-settings).
  - Очищены неиспользуемые инъекции connection и remote.
  - Синхронизированы декларации пакетов в package.json (dsh.client.inject = ["@deepseek-ai/dsh-client-locale", "@deepseek-ai/dsh-client-ui-settings", "@deepseek-ai/dsh-client-ui-slots"]) и клиентском рантайме (module.exports = { apply, inject: ['slots', 'locale', 'configForms'] }).
- **Безопасное разрешение области настроек (resolveScope)**:
  - Функция resolveScope(ctx) обращается к ctx.configForms.get('russian-lang') с обратной совместимостью через ctx.settingsScope.bind(...) для предыдущих версий ядра.
  - Предоставлен безопасный fallback stub-объект { getSnapshot, subscribe, set }, исключающий падения при запуске в изолированных тестовых средах без UI-сервисов.
- **Оптимизация бандла**:
  - Очистка однострочных комментариев перед обработкой регулярными выражениями устранила перехват обёртки загрузчика модулей.
  - Размер lib/client.js снижен до 153.4 KiB (запас более 10.4 KiB от лимита 160 KiB).
- **Регрессионные тесты**:
  - Добавлен тестовый набор test/client-activation.test.mjs (4 теста), верифицирующий регистрацию инъекций, активацию с configForms, фолбэк на старые ядра и безопасную работу при отсутствии сервиса.

### 2. Пакетная локализация DSH Core 0.1.7-alpha.1 и экосистемы плагинов (Issue #336, PR #337)
- **Локализация новых пространств имён ядра DSH**:
  - Полный перевод новых и расширенных модулей: trajectory, schedule, timeline, trajectory.catalog, sidebarFiles, sidebarTextpreview, context.
- **Локализация экосистемных плагинов GoodAndReady (10 плагинов)**:
  - dsh-cron — русские подписи задач, расписаний и истории запусков.
  - dsh-key-rotation — карточка ротации API-ключей, валидация и мониторинг.
  - dsh-auto-mode — подтверждения и диалоги автономного режима.
  - dsh-hooks — пресеты хуков, китайские системные шаблоны.
  - better-sidebar, dsh-model-search, dsh-kanban, dsh-session-control, dsh-context-lens, dsh-task-tracker.
- **100% покрытие интерфейса**:
  - 56 core namespaces (2 767 ключей) и 65 plugin namespaces (4 695 ключей).
  - 100.0% эффективное runtime-покрытие пользовательского интерфейса (7 574 ключа).

## 0.3.2 — 2026-09-21

Интерактивный Инспектор перевода и быстрые оверрайды на лету (Alt+Click / Alt+I), оптимизация производительности MutationObserver (debouncing & batching), защита HTTP-маршрутов словарей (405 Method Not Allowed) и оптимизация размера клиентского бандла.

### 1. Интерактивный Инспектор перевода и быстрые оверрайды (Issue #330, PR #331)
- **Точечный и постоянный инспектор**:
  - Точечный вызов: `Alt + Click` по любому элементу интерфейса открывает контекстный инспектор локализации.
  - Постоянный режим: горячая клавиша `Alt + I` включает визуальную рамку подсветки (`.rl-inspector-hover`) с бейджем источника и плавающий статус-виджет (`.rl-inspector-pill`) с кнопкой быстрого выхода.
- **Модальное окно инспектора (`#dsh-ru-inspector-modal`)**:
  - Точное определение ключа локализации, пространства имён и типа источника (ядро DSH, плагин, словарь прямого китайского DOM `zh-ru`, английский DOM `en-ru` или непереведённый текст).
  - 1-клик копирование системного ключа или форматированного Markdown-сниппета для баг-репорта.
  - Поле ввода пользовательского оверрайда с мгновенной мутацией DOM и сохранением в постоянные настройки (`scope.overrides`).
  - Кнопка сброса оверрайда и отката к исходному словарному значению.
- **Глобальное отражение оверрайдов**:
  - Пользовательские переопределения мгновенно применяются ко всем аналогичным текстовым узлам интерфейса через `queueZhWalk(0)` без перезагрузки страницы.
- **Управление оверрайдами в карточке настроек (Секция 4.5)**:
  - Кнопка включения/выключения инспектора (`Alt+I`).
  - Кнопки экспорта (`Экспорт JSON`) и импорта (`Импорт JSON`) настроенных оверрайдов.

### 2. Оптимизации глубокого аудита (Issues #325, #326, #327, #328, PR #329)
- **Защита HTTP-эндпоинтов словарей (Issue #325)**:
  - Эндпоинты `/dict/core`, `/dict/plugins`, `/dict/all`, `/dict/search` явно отклоняют неподдерживаемые HTTP-методы (POST, PUT, DELETE) статусом `405 Method Not Allowed` с заголовком `Allow: GET, HEAD`.
- **Оптимизация производительности `zhObserver` (Issue #326)**:
  - Внедрён адаптивный debounce (16 мс) и пакетная обработка мутаций DOM, предотвращающие лавинообразные вызовы при интенсивном рендеринге списков и стриминге LLM-токенов.
  - Исключена циклическая обработка внутренних служебных виджетов плагина (`#dsh-ru-inspector-*`, `.rl-lang-switch`, `.rl-typo-tooltip`).
- **Запас размера клиентского бандла (Issue #327)**:
  - Нормализация форматирования и структуры `lib/client.js` снизила размер до 153.7 KiB, обеспечив запас более 10 KiB ниже предела 160 KiB.
- **Верификационные тесты (Issue #328)**:
  - Добавлен тестовый набор `test/test_audit_optimizations.mjs` (4 теста), верифицирующий статус 405, оптимизацию наблюдателя, размер бандла и валидацию UI.

## 0.3.1 — 2026-09-20

Комплексный релиз: двухфазная ленивая загрузка словарей плагинов, клиентское кэширование Zero-FOUT с поддержкой ETag, реестр экосистемы GoodAndReady (61 плагин), интерактивное управление переопределениями и контекстный инспектор переводов (Alt+Click), двуязычный поиск в командной палитре (Ctrl+K), очеловечивание сетевых ошибок и локализация тостов, русская плюрализация и форматтеры чисел/дат/токенов в шаблонах, новые пресеты системных инструкций для агентов, CLI-инструмент разработки dsh-i18n, upstream-watcher с systemd-таймером и быстрые pre-commit хуки Lefthook.

### 1. Оптимизация загрузки и экосистема
- **1.1 Двухфазная ленивая загрузка словарей плагинов (Lazy Loading)** (Issue #284, PR #286):
  - Эндпоинт `/dict/core` отдает только базовые словари DSH Core (~135 КБ), гарантируя быстрый запуск интерфейса без задержек на передачу сотен килобайт редких плагинов.
  - Эндпоинт `/dict/plugins?names=...` догружает словари плагинов по мере обнаружения активных пространств имен.
  - Клиентский хук `loadPluginDict(namespace)` динамически подгружает и мерджит словари в рантайм `scope.load()`.
- **1.2 Клиентское кэширование Zero-FOUT & ETag** (Issue #285, PR #288):
  - Мгновенная синхронная инициализация переводов из `CacheStorage` / `localStorage` при загрузке страницы, исключающая мигание английского интерфейса (Flash of Untranslated Content).
  - Фоновая ревалидация с заголовком `ETag` и ответом `304 Not Modified` для минимизации трафика.
- **1.3 Реестр экосистемных плагинов DSH** (Issue #279, PR #282):
  - Каталог `ecosystem-plugins.json` (61 плагин GoodAndReady, 4 323 ключа).
  - Автоматическое разделение ядра и экосистемы в `check-coverage.py`.
  - Добавлена полная локализация `dsh-github-ops` (`ru-plugins/77-github-ops.json`, 20 ключей; Issue #287, PR #289).
- **Синхронизация актуальных ключей экосистемы GoodAndReady (Wave 2)** (Issue #308):
  - `dsh-task-tracker` (+65 ключей): полный интерактивный UI задачника (добавление, папки, модалки, приоритеты, фильтры, статус апдейтера).
  - `dsh-subscriptions` (+27 ключей): интерфейс авторизации и управления подписками (OAuth/Antigravity подсказки, подключение, статусы лимитов).
  - `dsh-clinebot` (+3 ключа): нотификации апдейтера (`update.available`, `update.done`, `update.failed`).
  - `dsh-gitea` (+1 ключ): подсказка вебхуков событий (`eventsEmptyHint`).
  - `dsh-messenger-gateway` (+1 ключ): индикатор сбоев API Telegram (`badge.api_degraded`).
  - `dsh-github-ops` (+6 ключей, Issue #311): параметры `tokenSource`, `maxRetries`, `reviewRules` и DOM-перевод кнопки и заголовка «Операции с GitHub».
  - **Синхронизация dsh-goal Power Suite и dsh-github-ops** (Issues #310, #311, #314):
  - `dsh-goal` (+13 ключей): управление бюджетом токенов, чекпоинты и сохранение артефактов в v0.3.0 Power Suite.
  - `dsh-github-ops` (+8 ключей): параметры `tokenSource`, `maxRetries`, `reviewRules`, `approvalMode`, `approvalModeHint` и заголовок карточки в DOM.
- **Полная синхронизация экосистемы GoodAndReady (Wave 3)** (Issue #316, +151 ключ):
  - Прямой AST-аудит всех 43 репозиториев экосистемы выявил и перевёл недостающие строки в 11 плагинах:
    - `dsh-image-gen` (+45 ключей): полный интерфейс настроек студии изображений (FAL, OpenAI-совместимый бэкенд, параметры очередей).
    - `dsh-server-monitor` (+26 ключей): управление серверами, SSH-генерация ключей, команды установки и автообновление.
    - `dsh-lanmode` (+21 ключ): управление именами/платформами устройств и встроенный апдейтер.
    - `dsh-voice` (+13 ключей): новые провайдеры STT (Sherpa/SenseVoice, Whisper, Voxtral) и словарь IT-сленга/жаргона.
    - `dsh-model-search` (+11 ключей): заголовок, описание и элементы интерфейса обновления.
    - `dsh-cron` (+10 ключей): встроенный модуль проверки и установки обновлений без SSH.
    - `dsh-task-tracker` (+9 ключей): контекстные действия по задачам, шаблоны промптов и возврат в чат.
    - `dsh-issue-reporter` (+7 ключей): обработка сбоев, копирование кодов и диагностика отчетов.
    - `dsh-model-sync` (+6 ключей): управление проверкой обновлений и перезапуском.
    - `dsh-grok-xsearch` (+2 ключа): индикаторы текущей и актуальной версий.
    - `dsh-agent-orchestrator` (+1 ключ): индикатор принятия результата субагента оркестратором.
  - Итоговое эффективное покрытие UI достигло **8 191 / 8 191 ключ (100.0%)** во всех 60 пространствах имён.

- **Синхронизация новых плагинов экосистемы GoodAndReady (Wave 4)** (Issue #321, PR #322):
  - `dsh-web-gateway` (31 ключ): новая отказоустойчивая цепочка веб-поиска и парсинга (`ru-plugins/78-web-gateway.json`).
  - `dsh-approval-gate` (42 ключа): правила безопасности shell и диалоги подтверждений (`ru-plugins/79-approval-gate.json`).
  - `dsh-hypotheses` (+19 ключей): review-интерфейс гипотез, факты, история пересчёта и Telegram-уведомления (`ru-plugins/70-hypotheses.json`).
  - `dsh-agentrouter`: скоуп-алиас `@goodandready-private/dsh-agentrouter` (`ru-plugins/73-agentrouter.json`).
  - Эффективное покрытие UI достигло **8 411 / 8 411 ключей (100.0%)** во всех 65 пространствах имён экосистемы.

### 2. Пользовательский опыт и интерактивные инструменты
- **2.1 Интерактивное управление переопределениями (Custom Overrides UI)** (Issue #296, PR #297):
  - Встроенный интерфейс в карточке настроек плагина для просмотра всех активных переопределений, добавления новых пар «пространство.ключ = перевод» и удаления записей с автоматическим сохранением через `scope.set('overrides', ...)`.
- **2.2 Контекстный инспектор переводов (In-Context Translation Inspector)** (Issue #298, PR #299):
  - Режим инспекции по Alt+Click на любом элементе интерфейса.
  - Модальное окно `#dsh-ru-inspector-modal`: отображение пространства имен, ключа, текущего русского перевода, оригинального английского текста и инлайн-форма для мгновенного добавления/редактирования переопределения.
- **2.3 Двуязычный поиск в командной палитре (Bilingual Command Palette)** (Issue #300, PR #301):
  - Фильтрация команд в палитре (Ctrl+K) по русскому названию, оригинальному EN названию и ID команды.
  - Автоисправление ошибочной раскладки клавиатуры (`ghbdtn` -> `привет`, `рудз` -> `help`) и фонетическая транслитерация.
- **2.4 Локализация сетевых тостов и очеловечивание ошибок** (Issue #302, PR #303):
  - Тост-обсервер ядра (`toastObserver`) перехватывает всплывающие ошибки и автоматически переводит их на понятный русский язык (`FETCH_FAILED`, `CORS_ERROR`, `WS_CLOSED`, `QUOTA_EXCEEDED`, `CONTEXT_OVERFLOW`, `MODEL_NOT_FOUND`).
  - Функция `formatErrorToast(err)` с подсказками к действию.

### 3. Лингвистический движок и агентские пресеты
- **3.1 Универсальная русская плюрализация (Pluralization)** (Issue #290, PR #291):
  - Алгоритм plural для русского языка (1 файл, 2 файла, 5 файлов) в функции `plural(count, forms, withCount)`.
  - Поддержка тегов `{count:plural:one,few,many}` в шаблонизаторе `fill()`. Экспорт в `runtime.plural`.
- **3.2 Форматтеры чисел, дат и токенов (Intl Formatters)** (Issue #292, PR #293):
  - Форматирование дат `{val:date}`, компактных чисел `{val:compact}` (12,5 тыс., 1,5 млн) и токенов `{val:tokens}` (1 234 токена).
  - Функции `formatDate`, `formatCompactNumber`, `formatTokens` в `lib/pure.js` и `runtime`.
- **3.3 Расширенные пресеты системных инструкций для агентов** (Issue #294, PR #295):
  - Добавлены готовые пресеты: «Ревьюер кода», «Архитектор ПО», «Наставник / Тьютор» с выбором в карточке настроек плагина.

### 4. Инструменты разработчика и автоматизация
- **4.1 Мониторинг релизов апстрима DSH (Upstream Release Watcher)** (Issue #280, PR #283):
  - Скрипты `scripts/upstream-watch.py` и `tools/upstream_check.py` для отслеживания upstream-релизов ядра DSH.
  - Настроен systemd user timer `dsh-upstream-check.timer` для автоматического периодического аудита.
- **4.2 Консольная утилита dsh-i18n для сторонних плагинов** (Issue #304, PR #305):
  - Исполняемый файл `bin/dsh-i18n.mjs` (`extract`, `validate`, `scaffold`, `--help`, `--version`).
  - Проверка соответствия терминов глоссарию (`glossary.json`), валидация плейсхолдеров, шаблонизация новых локализаций.
- **4.3 Pre-commit хуки Lefthook** (Issue #278, PR #281):
  - 5 быстрых параллельных проверок перед коммитом: `build-check`, `coverage`, `lint`, `pack-size`, `terms`.

### Качество и метрики
- Покрытие: **100.0%** по всем 7 902 ключам интерфейса.
- Тесты: **182/182 pass** (100.0%).
- Проверка размера пакета: все файлы `lib/` < 256 KiB, `lib/client.js` < 160 KiB.

## 0.3.0 — 2026-09-20

Минорный релиз: накопились изменения самого плагина локализации (посадка настроек на страницу плагина) и этапы синхронизации словарей, сделанные параллельно.

### Настройки плагина на странице «Плагины» (Issue #232, #273)

- **Карточка регистрируется в списочном слоте `plugins.item`** — именно его текущее ядро 0.1.6-alpha.2 рендерит как страницу плагина с настройками (подтверждено на живом UI: так же показывают себя `dsh-agentrouter` и `dsh-agent-orchestrator`). Ключ: `id: 'russian-lang'`, `order: 60`, **статичный** `label` — обращений к локали в label нет, потому что прокси ядра бросает на самом обращении к свойству и это роняет весь клиентский батч.
- Посадки `plugins.row.config` и `settings.plugin.item` сохранены как фолбэки для ядер, которые рендерят только их.
- Правка сделана в шаблоне `build.py`, `lib/client.js` пересобран (проверка лимита DSH Store: все файлы `lib/` < 256 KiB — OK).

### Сохранена ручная правка словаря

- `lib/locales/plugins/72-server-monitor.json` содержит **66 ключей** (против 56 в компактном выводе `ru-plugins/`). Сборка перезаписывает этот файл, поэтому он восстановлен из бэкапа — ключи не потеряны.

### Тесты

- Тест релиза больше не прибит к одной версии молча: он сверяет `package.json` **и** то, что заголовок `CHANGELOG.md` называет ту же версию — забытая запись в changelog теперь падает тестом.
- `npm test` — 144/144.

### Что вошло в этот же релиз из параллельных этапов

- Этап 4 — синхронизация словарей (`a0afafa`).
- Этап 5 — устранение техдолга (`61a425a`).
- Этап 6 — сверка волны плагинов и 100 % покрытие (`9fb393d`).

## 0.2.26 — 2026-09-19

### Настройки плагина на странице «Плагины» (Issue #232)

- Карточка регистрируется **сначала** в посадке строки `plugins.row.config` с ключом
  `@goodandready/dsh-russian-lang#russian-lang`: текущее ядро 0.1.6-alpha.2 рендерит
  настройки плагина именно там, а слот `settings.plugin.item`, куда мы писали, оно
  не рендерит вовсе. Прежняя посадка сохранена фолбэком для старых ядер.
- Карточка стала view-aware: `view: 'summary'` — однострочник (строка статуса),
  `view: 'page'` — форма без нашей шапки (класс `rl-page-seat`, кнопка-заголовок
  скрыта, тело раскрыто) — страница сама рисует заголовок, иконку, крошку и отступы.
- Правка сделана в шаблоне `build.py`; `lib/client.js` пересобран.
- Сохранена правка локали `lib/locales/plugins/72-server-monitor.json` (66 ключей
  вместо 56 из `ru-plugins/`) — сборка её перезаписывает компактным выводом, файл
  восстановлен из бэкапа, ключи не потеряны.

## 0.2.25 — 2026-09-18

### Пакетная локализация ядра DSH v0.1.6-alpha.2 (325 ключей) и обновлений плагинов (92 ключа) (Issue #246, #234)
- **Поддержка и полная локализация ядра DSH v0.1.6-alpha.2 (325 новых ключей)**:
  - `pluginManager` (155 ключей): новый менеджер плагинов, добавление пакетов по имени/git/path, карточки статусов и фаз компонентов, встроенный pnpm-терминал с историей вывода и кодами завершения, гейт подтверждения сборочных скриптов зависимостей (`installApproval*`).
  - `agent-team` (35 ключей): мультиагентные команды (участники, статусы, реестр общих задач, блокирующие зависимости, области записи).
  - `deliverables` (34 ключа): ревью изменённых файлов шага, раздельный и объединённый diff-режимы, перенос строк, открытие в системном приложении.
  - `sidebarBrowser` (22 ключа): встроенный браузер страниц HTTP(S), управление песочницей и навигацией.
  - `sidebarOffice` (17 ключей): встроенный просмотрщик документов Office, обработка отсутствующих шрифтов и ошибок конвертации.
  - `settings` (18 ключей): процесс десктопных обновлений, сетевые статусы и обработка сбоев.
  - `settings.plugins` (15 ключей): настройка лимитов параллелизма и глубины рекурсии субагентов.
  - `plan` (13 ключей): превью планов в сайдбаре, навигация по шагам.
  - `settings.models` (3 ключа), `settings.pluginInventory` (3 ключа), `trajectory` (3 ключа), `conversation` (2 ключа), `subagent` (2 ключа), `model` (1 ключ), `reference` (1 ключ), `workspace` (1 ключ).
- **Локализация новых возможностей плагинов (92 ключа)**:
  - `dsh-context` (62 ключа): новый модуль аналитики сессий Context Insights (`ov.*`), KPI активных сессий, графики активности и тепловая карта, сортировка по токенам и контексту.
  - `@goodandready/dsh-dsml-artifact-guard` (10 ключей): панель настроек, санитайзер потоков и проверка обновлений.
  - `dsh-vision-bridge` (10 ключей): блок версий и автообновления для строкового namespace.
  - `dsh-cost-meter` (7 ключей): секция обновлений для строкового namespace.
  - `settings.commandcode` (2 ключа): настройка количества повторных попыток транспорта.
  - `@goodandready/dsh-time-machine` (1 ключ): кнопка мгновенного обновления.
- **Гигиена репозитория (Issue #234)**:
  - Удалены случайные архивные `.tgz` из корня репозитория.
  - Упаковка артефактов при сборке и тестировании выполняется во временный каталог `/tmp`.

## 0.2.19 — 2026-09-16

### Добавлено и улучшено (753 новых ключа, 7 902 ключа в UI, 100.0% покрытие)
- **Поддержка и полная локализация ядра DSH v0.1.6-alpha.1 (88 новых ключей)**:
  - `sidebarTerminal` (27 ключей): встроенный интерактивный терминал в правой панели, выбор шелла, PTY-переподключение, обработка обрывов соединения и восстановление сессий.
  - `settings.archivedSessions` (15 ключей): панель архивированных сессий, поиск, фильтрация по времени и восстановление бесед.
  - `command` (14 ключей): палитра быстрых системных команд (`/goal`, `/plan`, `/feedback`, `/compact`, `/permission`, `/export`).
  - `permission.access` (9 ключей): экспериментальный режим безопасности `Auto review (EXP)` без песочницы с верификацией вызовов инструментов той же моделью и диалог подтверждения рисков.
  - `trajectory` (7 ключей): визуализатор траектории выполнения задач (перенос строк, вкладки Код/Вывод, копирование, оригинальный JSON).
  - `settings.agentPreset` (6 ключей): переключение режимов агентов (Standard, PTC, Creator, Minimal), Beta-бейдж и параметры по умолчанию.
  - `conversation` & `chat` (5 ключей): статусы отклонения инструментов Auto review, кнопка прикрепления файлов, формат длительности диалога с часами.
  - `settings.models` (3 ключа): официальные эндпоинты протокола сообщений DeepSeek Messages / Anthropic.
- **Полная локализация плагинов каталога GoodAndReady (519 новых ключей)**:
  - `@goodandready/dsh-subscriptions` (138 ключей): OAuth-авторизация подписок, перехват токенов loopback, маскировка email для стримов (`privacyMask`), ротация, лимиты и локальный фолбэк.
  - `@goodandready/dsh-tts` (137 ключей): док синтеза речи в чате, кэш аудио (LRU/MB), каскад провайдеров, circuit breaker, автоопределение языка фразы.
  - `@goodandready/dsh-image-gen` (134 ключа): студия генерации изображений, 5 вкладок настроек, галерея в сайдбаре и BetterSidebar, карточки 12 инструментов.
  - `@goodandready/dsh-voice` (90 ключей): голосовой ввод, распознавание Whisper/SenseVoice, инъекция глоссария контекста из редактора, подавление тишины.
  - `@goodandready/dsh-key-limits` (20 ключей): плавающий drag-and-drop виджет лимитов API-ключей, ручная сортировка аккаунтов, периоды опроса.
- **Локализация экосистемных плагинов DSH (146 новых ключей)**:
  - `panel.commandcode` & `settings.commandcode` (51 ключ): полноразмерный дашборд тарифов Command Code, плавающие окна квот, баланс кредитов, карточка в сайдбаре.
  - `settings.pluginConsole` (28 ключей): гейт совместимости плагинов (Compat Gate), автоотключение несовместимых, разблокировка адаптированных, панель обновления и отката DSH Framework.
  - `@goodandready-private/dsh-plugin-notify` (25 ключей): новый плагин вебхуков в Feishu, WeCom, DingTalk, Slack, Discord, DND-периоды.
  - `@goodandready/dsh-agent-loop-guard` (18 ключей): телеметрия защиты от зацикливания, dry-run режим аудита, лимиты строгих инструментов.
  - `config-manager` (15 ключей): многоуровневая ротация резервных копий и снимков (Retention policy — keep last/monthly/yearly).
  - `dsh-context`, `dsh-market`, `dsh-agent-orchestrator` (9 ключей).
- **Карточка настроек плагина `SettingsCard`**:
  - Обновлён статус экосистемы: синхронизация с **DSH v0.1.6-alpha.1**.
  - Бейдж покрытия обновлён до **🟢 100% (7,902 ключа)**.

## 0.2.18 — 2026-09-15

### Hotfix: Стабильность живой типографики, защита курсора и выделения текста (Issue #198, PR #199)
- **Устранение прыжков каретки и реверсивного ввода в `contenteditable` (Issue #198)**:
  - Полностью удалены вызовы `formatInputLive` и деструктивный `setComposerText` с `setTimeout(..., 15)` из события `input`.
  - Живая типографика переведена на локальную и нативную подстановку в момент `keydown`:
    * Двойной дефис `--` заменяется на длинное тире `—` через `delete` + `document.execCommand('insertText', false, '—')`.
    * Кавычки `"` автоматически заменяются на парные французские ёлочки `«...»` в контексте каретки.
    * Пробел после однобуквенных и коротких русских предлогов (`в `, `и `, `с `, `к `, `о `, `у `, `а `, `не `) локально заменяется на неразрывный пробел `\u00A0`.
  - Полностью сохраняется нативная история отмены `Ctrl+Z` / `Ctrl+Y` и фокус текстового редактора Lexical.
- **Защита выделения (Selection) и потокового вывода ответа (Streaming)**:
  - `typoNode` исключает активные поля ввода (`[contenteditable="true"]`, `[role="textbox"]`, `[data-composer-input]`), контейнеры со стримингом токенов (`[data-chat-flow-status="running"]`, `.dsw-turn-running`, `[data-streaming="true"]`), а также текстовые узлы, пересекающиеся с активным пользовательским выделением (`window.getSelection()`).
  - `typoObserver` избавлен от мутаций по `characterData: true` и отслеживает только структурное появление узлов (`childList: true`), предотвращая схлопывание выделенного пользователем текста во время генерации ответа LLM.
- **Интеллектуальная горячая клавиша Alt+L**:
  - При наличии выделенного фрагмента текста корректор раскладки преобразует только выделенный фрагмент.
  - При отсутствии выделения текст поля преобразуется целиком с сохранением и восстановлением точного смещения курсора через DOM TreeWalker (`getCaretCharacterOffset` / `setCaretCharacterOffset`) без асинхронных задержек.
  - Добавлена защита фрагментов инлайн-кода (`` `...` ``) и блоков кода (```...```).
- **Безопасная интеграция в Cordis 4**:
  - Маршрутизация хостового эндпоинта автообновления `/api/dsh-russian-lang/update` обёрнута в `ctx.inject(['webServer'], (wctx) => ...)`.
- **Экосистема локализации**:
  - 10 542 ключа интерфейса в 116 пространствах имён ядра DSH v0.1.5-rc.2 и плагинов.
  - 119/119 модульных и регрессионных тестов пройдено.

## 0.2.17 — 2026-09-15

### Добавлено и улучшено (371 новый ключ, 7 149 ключей в UI, 100.0% покрытие)
- **One-Click обновление плагина из настроек (`/api/dsh-russian-lang/update`)**:
  - Реализован хостовый эндпоинт безопасного автообновления по стандарту скиллов `dsh-plugin-authoring` и `dhs-plugin-release-workflow`.
  - В карточку настроек `SettingsCard` добавлен интерактивный блок: автоматическая проверка свежей версии в npm, индикация бейджей `Актуальная версия` / `Доступно обновление`, и кнопка `🔄 Обновить плагин в 1 клик` с защитой от двойных кликов и уведомлением о перезапуске DSH.
- **dsh-lanmode (111 новых ключей)**:
  - Полный перевод мобильного интерфейса и карточки настроек: быстрое подключение со смартфона/планшета по QR-коду, принудительный десктопный режим, разграничение прав по подсетям (admin/guest), мобильные жесты, PWA-манифест, телеметрия задержек и управление авторизованными устройствами.
- **dsh-moa (87 новых ключей)**:
  - Полная русификация Mixture of Agents v0.2.14: параллельная генерация, роли моделей-кандидатов (Minimalist/Ponytail, Robustness, Performance, Tester), слепое рецензирование (Blind Review), советы куратора, цепочки резервных судей, экспорт таблицы побед в CSV/JSON.
- **dsh-gitea (48 новых ключей)**:
  - Перевод Git Inspector Drawer (просмотр состояния веток, коммитов и незафиксированных изменений), визуального графа коммитов и статусов CI, входящих вебхуков с проверкой HMAC-подписи и фонового триажа задач.
- **dsh-issue-reporter (47 новых ключей)**:
  - Перевод всех интерфейсов релиза v0.1.1: навигационные вкладки (Каталог, Редактор, Мои отчёты, Авторизация), перетаскивание скриншотов драг-н-дропом, вставка `Ctrl+V`, системная диагностика, AI-оптимизация описания и быстрый репорт сбоев.
- **dsh-messenger-gateway (29 новых ключей)**:
  - Перевод шлюза Telegram: телеметрия пинга до Telegram API, счётчики сообщений, проверка связи (Smoke Test), управление разовыми кодами сопряжения.
- **dsh-usage-guard (32 новых ключа)**:
  - Локализация панели защиты токенов: переключение режимов Active Repair / Audit Only, лимиты сглаживания всплесков, живая телеметрия спасённых сессий и проверка обновлений.
- **dsh-cron (11 новых ключей)**:
  - Поддержка персистентного `targetSessionId` для сохранения непрерывности контекста между запусками и монтирование пресетов агентов из `dsh-agent-presets`.
- **dsh-model-search & dsh-agent-orchestrator (6 новых ключей)**:
  - Локализация поиска моделей и нового мульти-агентного оркестратора на базе DAG.

### Инфраструктура и качество
- 108/108 автоматических тестов пройдены, 0 регрессий.
- 100.0% ручное выверенное покрытие без машинных черновиков (0 MT drafts).
- Добавлен полный unit-тест `test/test_v0217_updater_and_plugins.mjs`.

## 0.2.16 — 2026-09-14

### Локализация новых плагинов и функциональных расширений (6 808 ключей интерфейса)
- **Синхронизация с актуальным состоянием экосистемы DSH**:
  - Добавлено **179 новых ключей** (всего в экосистеме локализовано **6 808 ключей**: 1 225 ядра + 5 583 плагинов).
  - 100.0% покрытие, 0 непереведённых строк, 108/108 автоматических тестов пройдено.
- **Новые переведённые плагины и модули**:
  - **`@goodandready/dsh-model-sync`** (117 ключей, `ru-plugins/60-model-sync.json`):
    * Полная русская локализация синхронизатора каталогов моделей провайдеров.
    * Планировщик фоновой синхронизации, подтверждение удаления устаревших моделей, история запусков и откат снимков каталога.
    * Пакетная проверка доступности моделей (*Batch Try Selected*), фильтрация по максимальной цене и минимальному контексту.
    * Экспорт и импорт настроек в JSON, управление алиасами моделей (*Model Aliases*).
  - **`@goodandready/dsh-issue-reporter`** (46 ключей, `ru-plugins/61-issue-reporter.json`):
    * Полная русская локализация инструмента репорта багов на GitHub.
    * Авторизация GitHub Device Flow, поиск дубликатов, безопасный предпросмотр (*Sanitized Preview*) с маскированием секретов.
    * Группировка нативных и сторонних плагинов DSH, валидация полей и отправка issues.
  - **`@goodandready/dsh-model-search`** (4 ключа, `ru-plugins/62-model-search.json`):
    * Локализация подсказок поиска моделей и провайдеров, поддержка русских фонетических алиасов и переключения раскладки.
  - **`dsh-grok-xsearch`** (обновление, `ru-plugins/58-grok-xsearch.json`):
    * Добавлены переводы метрик кэширования: попадания (*Hits*), промахи (*Misses*), эффективность кэша (*Hit rate*).
    * Переведены заголовки карточки настроек, сообщения статуса подключения OAuth и подсказки инструментов xAI.
  - **`@goodandready/dsh-session-control`** (обновление, `ru-plugins/45-session-control.json`):
    * Перевод массовых действий: пакетная архивация сессий навсегда, пакетный экспорт в Markdown.
    * Новые фильтры диалога быстрого перехода Alt+K (*Все / Закреплённые / Метки / Архив*).
    * Счётчик ходов диалога (*{n} сообщений*) и быстрое клонирование диалога с сохранением настроек (*Fork Clean*).

## 0.2.15 — 2026-09-13

### Масштабная локализация экосистемы DSH (6 629 ключей интерфейса)
- **Синхронизация с обновлениями и новыми плагинами прода**:
  - Выполнен детальный аудит рабочего профиля `web` DSH (`v0.1.5-rc.1` / `v0.1.5-rc.2`).
  - Добавлено **627 новых ключей** в 16 пространствах имён плагинов.
  - Общее эффективное покрытие интерфейса выросло с 6 002 до **6 629 ключей** (1 225 ядро + 5 404 плагины).
  - 100.0% покрытие, 0 непереведённых строк, 100% соответствие утверждённому глоссарию DSH.
- **Детализация переведённых модулей и плагинов**:
  - `dsh-cron` (+319 ключей):
    * Полная локализация планировщика задач DSH: дашборд расписаний, фильтры, создание задач (Shell / LLM / One-shot).
    * Интерактивный мастер тюнинга с AI, pre-flight проверки надёжности (HTTP 2xx, Shell, свободное место на диске).
    * Поддержка Heartbeat, льготных периодов, групп параллелизма (concurrency group), самоисцеления (*self-healing runbook*).
    * Симуляция запусков (Dry Run), архив выполнений и многоканальная доставка отчётов (Telegram, Discord, Slack, ntfy, Bark, PushPlus, голос, Gitea).
  - `@goodandready/dsh-live-canvas` (+118 ключей):
    * Интерактивный канвас для предпросмотра HTML/Tailwind/React/SVG в реальном времени.
    * Инспектор элементов, доработка интерфейса с AI (*AI Refine Element*), каталог современных дизайн-блоков.
    * Режим Drag & Drop перестановки секций, мобильный предпросмотр по QR-коду, генератор артефактов (вайрфреймы, дорожные карты, диаграммы) и встроенная мини-консоль.
  - `dsh-goal` (+50 ключей):
    * Автономный режим достижения целей (Goal Mode): баннер цели, этапы декомпозиции, оценка времени (ETA), токены и итерации.
    * Живое подруливание (*Live Steering / Nudge*), прерыватель серии ошибок (*Tool-Failure Breaker Limit*), экспорт отчёта для PR/Markdown, копирование git diff.
  - `dsh-grok-xsearch` (+73 ключа):
    * Поиск и аналитика трендов в сети X (Twitter) через Grok: режим углублённого поиска (Deep Search), новостная повестка, тональность, настройки OAuth и кэша.
  - `skills-manager` (+32 ключа):
    * Менеджер навыков: фильтрация перекрытых и диагностических навыков, разделение областей видимости (глобальные / проект / корзина), выбор источников.
  - `@goodandready/dsh-shadow-auditor` (+23 ключа):
    * Контроль целостности файлов (*File Integrity*), живой журнал перехватов и аудита (*Audit Trail*), политика блокировки опасных shell-команд (Enforce / Audit Only).
  - `dsh-kanban` (+20 ключей):
    * Поддержка эпиков (*Epics & Subtasks*), декомпозиция задач, создание связанных задач в Gitea, живая консоль агента (*Live Console*).
  - `dsh-key-rotation` (+16 ключей):
    * Живая лента событий ротации (*Live Event Stream*), превентивная защита от лимитов (Rate-Limit Guard), стратегии маршрутизации (Lowest-Latency, Least-Loaded).
  - `@goodandready/dsh-agent-loop-guard` (+14 ключей):
    * Защитные лимиты циклов вызова инструментов и ответов ассистента: детекция зацикливания, блокировка дубликатов, ограничение повторов строк.
  - `dsh-remote-workspace` (+14 ключей):
    * Вкладка управления переменными `.env`, импорт хостов из `~/.ssh/config`, быстрый Tarball Stream, экспресс-диагностика хоста (OOM, порты, диск).
  - `dsh-cost-meter` (+13 ключей):
    * Экономия на кэше контекста (*Context cache saved*), расходы по моделям, валидация часовых поясов IANA.
  - `dsh-vision-bridge` (+10 ключей):
    * Телеметрия канала, анализ качества и размытия изображений (дисперсия Лапласа), попиксельное сравнение UI (*Pixel Diff*).
  - `dsh-context` (+7 ключей):
    * Подсказки по тарификации пиковых/ночных окон DeepSeek, сортировка инструментов по частоте вызовов.
  - `@goodandready/dsh-time-machine` (+6 ключей):
    * Восстановление отдельных файлов из контрольной точки (*Restore File*), список изменённых файлов.
  - `config-manager-sync` (+6 ключей):
    * Сводная статистика истории синхронизации (снапшоты, автосинк, сбои).
  - `@goodandready/dsh-context-lens` (+4 ключа):
    * Порог предупреждения по бюджету токенов (%) и управление путями фокуса (*Active focus*).
- **Визуальные метрики и оформление**:
  - Карточка настроек SettingsCard обновлена: «🟢 100% (6,629 ключей)» и блок «5 404 ключа плагинов».
  - Перегенерирован промо-баннер `docs/media/banner.jpg` (`DSH v0.1.5-RC.1 · 6,629 KEYS`).
- **Тестирование и контроль качества**:
  - 108/108 автоматических unit-тестов успешно пройдены (`pnpm test`, 100% pass).
  - 100% сохранение всех оригинальных плейсхолдеров и типографики («ёлочки», тире, неразрывные пробелы).

---

## 0.2.14 — 2026-09-12

### Полная локализация масштабного обновления прода (6 002 ключа интерфейса)
- **Синхронизация с обновлениями экосистемы плагинов**:
  - Выполнен детальный аудит рабочего профиля `web` DSH (`v0.1.5-rc.1` / `v0.1.5-rc.2`).
  - Добавлено **272 новых ключа** в 9 пространствах имён, общее эффективное покрытие интерфейса преодолело рубеж в 6 000 и составило **6 002 ключа** (1 225 ядро + 4 777 плагины).
  - 100.0% покрытие, 0 непереведённых строк, 100% строгое соблюдение глоссария терминов DSH.
- **Детализация переведённых модулей**:
  - `dsh-config-manager` (+145 ключей):
    * Новый раздел **Overview**: индикаторы здоровья конфигурации, статусы бэкапов/снапшотов, расписание и удалённая синхронизация.
    * Быстрые действия: экспорт в переносимый ZIP-архив, импорт/восстановление, мгновенный снимок безопасности.
    * Лента недавней активности (Recent activity) с подробными статусами операций и фильтрацией.
    * 6-шаговый мастер импорта (`Выбор -> Анализ -> Предпросмотр и решение -> Подтверждение -> Выполнение -> Готово`) с анализом совместимости разделов, путей и секретов.
    * Диагностика источника списка плагинов и профилей окружения DSH.
  - `dsh-remote-workspace` (+44 ключа):
    * Раздел **Контейнеры Docker / Podman**: мониторинг статусов, портов, образов, просмотр логов, перезапуск и остановка контейнеров.
    * Живые метрики хоста в реальном времени: нагрузка ЦП %, ОЗУ % (использовано / всего), диск %, аптайм.
    * Прямая передача сервер-сервер (*Server-to-Server Direct Transfer*).
    * Встроенный веб-терминал (Web Terminal PTY), удалённый файловый менеджер и предпросмотр файлов с сохранением.
    * Поддержка Jump-хостов (бастионов) и защита секретов `.env Vault`.
  - `dsh-key-rotation` (+28 ключей):
    * Информативная шапка пулов ротации с бейджами активных пулов и настроенных ключей.
    * Массовые операции: пакетный таймаут (bulk cooldown), применение к выбранным, удаление с подтверждением.
    * График распределения нагрузки по ключам, рейтинг надёжности провайдеров (*health score*), статус паузы по лимиту бюджета.
  - `config-manager-sync` (+19 ключей):
    * Статусы и уведомления каналов синхронизации (Git / WebDAV), предпросмотр push/pull, автосинхронизация и загрузка удалённых снимков.
  - `dsh-cost-meter` (+12 ключей):
    * Порог бюджета сессии в USD с цветовой индикацией превышения.
    * Уведомления и таймер скидочных окон (50% off-peak discount).
    * Кнопка немедленной синхронизации каталога цен и копирование сводки расходов.
  - `dsh-context` (+8 ключей):
    * Подсчёт сообщений пользователя (*Human Inputs*), варианты размещения в UI (вкладка / боковая панель / везде), предпросмотр файлов в боковой панели.
  - `dsh-vision-bridge` (+7 ключей):
    * Настройки маршрутизации и качества, диапазон лимита вложений (1–32), скрытие инструментов для моделей с нативным зрением.
  - `config-manager-market` (+7 ключей):
    * Обновление каталога витрины конфигураций, уведомления об импорте, подсказка о необходимости перезапуска.
  - `betterSidebar` (+2 ключа):
    * Индикатор ожидания ответа в терминале и кнопка пропуска ожидания.
- **Визуальные метрики и оформление**:
  - Карточка настроек SettingsCard обновлена: «🟢 100% (6,002 ключа)» и блок «4 777 ключей плагинов».
  - Перегенерирован промо-баннер `docs/media/banner.jpg` (`DSH v0.1.5-RC.1 · 6,002 KEYS`).
- **Тестирование и контроль качества**:
  - 108/108 автоматических unit-тестов успешно пройдены (`pnpm test`, 100% pass).
  - 100% сохранение всех оригинальных плейсхолдеров и типографики («ёлочки», тире, неразрывные пробелы).

---

## 0.2.13 — 2026-09-10

### Локализация обновлённых плагинов прода (5 765 ключей) и расширение экосистемы
- **Синхронизация с обновлениями плагинов на продакшене**:
  - Выполнен полный срез и анализ обновлений рабочего профиля `web` DSH (`v0.1.5-rc.1`).
  - Добавлено **100 новых ключей** в 5 ключевых плагинов экосистемы, общее эффективное покрытие интерфейса выросло до **5 765 ключей** (1 225 ядро + 4 540 плагины).
- **Детализация переведённых модулей**:
  - `dsh-cost-meter` (+23 ключа): динамический каталог цен OpenRouter, мониторинг расходов по временным окнам со скидками (off-peak), статистика и диагностика синхронизации.
  - `dsh-key-rotation` (+23 ключа): триггеры переключения ключей при сбоях (failover), коды ошибок, модальные окна сброса таймаутов блокировки (cooldown), бэкап/снимки пулов ключей и распределение трафика.
  - `dsh-remote-workspace` (+33 ключа): управление защищёнными SSH-туннелями и пробросом портов (open/stop/routes), замер задержки соединения (latency probe), локальное зеркалирование директорий.
  - `@goodandready/dsh-shadow-auditor` (+19 ключей): статический анализатор кода (SAST), сканер prompt-инъекций, политика безопасности Diff Gate (Предупреждение / Блокировка / Отключено) и журнал аудита.
  - `dsh-context` (+2 ключа): копирование обогащённого контекста и подтверждение («Текст скопирован»).
- **Визуальные метрики и оформление**:
  - Карточка настроек SettingsCard обновлена до актуальных метрик: индикатор «🟢 100% (5,765 ключей)» и блок «4 540 ключей плагинов».
  - Обновлён промо-баннер `docs/media/banner.jpg` со сводкой `DSH v0.1.5-RC.1 · 5,765 KEYS` (1,225 ядро, 4,540 плагины).
- **Контроль качества и тестирование**:
  - 108/108 автоматических тестов успешно пройдены (100% pass).
  - 100% строгое соответствие глоссарию терминов DeepSeek Harness (15/15 правил).
  - 0 непереведённых ключей (100.0% покрытие).

---

## 0.2.12 — 2026-09-10

### Визуальная унификация по стандарту dsh-clinebot и комплексный аудит стабильности
- **Визуальный редизайн карточки настроек (стандарт `dsh-clinebot`)**:
  - Архитектура карточки переведена на блочную структуру секций (`.rl-section-card`, `.rl-grid-2`, `.rl-stat-box`, `.rl-hotkey-box`, `.rl-preview-box`) с полной поддержкой темной и светлой тем через токены `--dsw-alias-...`.
  - Информативная шапка со статус-бейждами (`🟢 RU активен` / `⚪ EN`, `🟢 100% (5,688 ключей)`, `⚡ Smart UX`).
  - **Секция 1: Язык интерфейса** — нативный переключатель языка с индикацией статуса и подсказкой о быстром переключателе в шапке сессии.
  - **Секция 2: Умная типографика и ввод (Smart UX)** — сетка 2×2 карточек настроек (типографика вывода, живая типографика инпута, буква «ё», слэш-алиасы команд) и плашка горячей клавиши `Alt+L`.
  - **Секция 3: Системный промпт агента** — тумблер активации, выпадающий список стилей («Технический эксперт», «Технический писатель», «Лаконичный режим») и интерактивный блок предпросмотра промпта.
  - **Секция 4: Покрытие экосистемы и поддержка** — метрики словарей (99 пространств имён, 1 225 ядро, 4 463 плагины), автозаполненная форма отправки отчёта об ошибке на GitHub и подсказки по экспорту Markdown и переводу сообщений.
- **Аудит кода и устранение устаревших фрагментов**:
  - Полностью удалены неиспользуемые ссылки на Alt+T (транслитерация была вырезана в 0.2.10 по запросу пользователя).
  - Скорректированы формулировки описания живой типографики.
  - Расширена карта человекочитаемых системных ошибок `ERROR_MAP` в `lib/pure.js` (`ECONNRESET`, `ENOSPC`, `400 Bad Request`, `504 Gateway Timeout`).
  - Улучшена обработка ошибок Axios/Fetch в `humanizeError` с извлечением статуса из `err.response.status`.
- **Тестовое покрытие и стабильность**:
  - Добавлен новый тестовый модуль `test/test_stability_audit.mjs` (13 всесторонних тестов, покрывающих обработку POSIX и HTTP ошибок, форматирование валют, относительное время, морфологическое склонение существительных во всех падежах, сохранение регистра, fuzzy-поиск, генерацию URL репортов и Markdown-экспорт).
  - Общий объем тестов вырос до 108/108 (100% pass).
  - Покрытие интерфейса сохранено на уровне 100.0% (5,688 ключей UI, 0 непереведённых).

---

## 0.2.11 — 2026-09-10

### Поддержка DSH v0.1.5-rc.1, Центра оформления (Skin Center) и Витрины (Workshop)
- **Ядро DSH v0.1.5-rc.1 (1,225 ключей, 100% покрытие)**:
  - Актуализированы словари ядра под релиз-кандидат `v0.1.5-rc.1`.
  - Добавлено описание рабочей области в проводнике файлов (`sidebarFiles.guide.description`).
- **Поддержка Центра оформления и скинов (`skinCenter`, 93 ключа)**:
  - Полная русификация настроек тем и фонового оформления:
    - Интеграция с Wallpaper Engine (живой рендеринг видео, веб и 3D-сцен, сканирование папок, регулировка громкости звука видео-обоев, автопауза при сворачивании);
    - Размытие фона и карточки ввода (индивидуальное гауссово размытие для пустой сессии и сессии с сообщениями);
    - Настройка прозрачности всплывающих сообщений (баблов);
    - Интерактивная живая примерка тем в реальном времени с возвратом исходного вида и сохранением между перезапусками;
    - Редактор пользовательской палитры (настройка акцента, фона, текста и контраста для светлой и тёмной тем).
- **Поддержка витрины Workshop (`dsh-web-ui-market`, 54 ключа)**:
  - Перевод каталога расширений dsh-market.com: вкладки *Скины*, *Питомцы*, *Плагины*;
  - Быстрая установка в 1 клик, копирование команд терминала, разрешение конфликтов существующих каталогов, фильтрация и система лайков;
  - Карточка настроек витрины в общих настройках DSH.
- **Обновления плагинов BetterSidebar и DSH Context**:
  - `betterSidebar` (7 новых ключей): подсказки-описания для боковых панелей (Git, Терминал, Субагенты, Сайдчат, Файлы, Браузер) и предупреждение о ненахождении настроенного шелла терминала.
  - `dsh-context` (3 новых ключа): детализация таймингов модели (*Размышление*, *Ответ*, *Аргументы инструментов*).
- **Эффективное покрытие интерфейса**:
  - **5,688 ключей UI** (1,225 ядро + 4,463 в 40+ плагинах, 0 непереведённых, 100% ручная вычитка).

---

## 0.2.10 — 2026-09-10

### Полная локализация ядра DSH v0.1.5-alpha.2 и новых плагинов экосистемы
- **Ядро DSH v0.1.5-alpha.2 (1,224 ключа, 100% покрытие)**:
  - Добавлена полная русская локализация для всех 6 новых встроенных просмотрщиков боковой панели:
    - `documentMarkdown` — просмотрщик документов Markdown (структура, оглавление, рендеринг);
    - `documentHtml` — безопасный предпросмотр веб-страниц и HTML-разметки;
    - `sidebarImage` — просмотрщик графических форматов, масштабирование, метаданные;
    - `sidebarPdf` — встроенный просмотр PDF-файлов и навигация по страницам;
    - `sidebarCodePreview` — подсветка исходного кода, нумерация строк и быстрые действия;
    - `sidebarDocumentPreview` — универсальный просмотрщик документов и таблиц.
  - Локализованы новые контекстные действия над артефактами в `deliverables` («Показать в папке» / `produced.showInFolder`).
  - Локализованы диалоги заметок и обратной связи `feedback` (добавление примечаний к шагам диалога).
  - Обновлены селекторы моделей и конфигурация воркспейса под alpha.2.

- **Плагины экосистемы (4,306 ключей, 100% покрытие)**:
  - Добавлен перевод нового плагина **`dsh-remote-workspace`** (45 ключей): подключение к удалённым серверам, управление SSH-хостами, синхронизация файлов, индикаторы статуса соединения.
  - Обновлены переводы плагинов: `@goodandready/dsh-time-machine` (диффы, просмотр снимков и откат изменений), `dsh-context` (детализация кэша и времени ответа), `settings.pluginConsole` (синхронизация индекса).
  - Общее эффективное число ключей в UI достигло **5,530** (1,224 в ядре + 4,306 в плагинах, 0 непереведённых, 100.0% покрытие).

### Стабилизация Smart Russian UX (Lexical Composer, MD Export & Turn Translation)
- **Стабилизация поля ввода Lexical (DSH v0.1.5)**:
  - Поддержка набора как в современных редакторах Lexical contenteditable (`[data-composer-input="true"]`), так и в классических `<textarea>`.
  - Удалён экспериментальный перехватчик многоточия (`...`), исключено залипание и блокировка ввода стандартных точек.
  - Удалены горячие клавиши фонетической транслитерации (Alt+T, Ctrl+Q) и кнопка `[⇄ T]` в пользу чистого нативного ввода; сохранён надёжный индикатор активной раскладки `[RU]`/`[EN]` с конвертером ошибочной раскладки (<kbd>Alt+L</kbd>).
- **Экспорт сессии в Markdown (`📥 MD`)**:
  - Кнопка экспорта `📥 MD` в заголовке сессии выгружает структурированный, красиво оформленный `.md` файл с метаданными диалога, ролями сообщений и временными метками.
- **Инлайн-перевод сообщений ассистента (`RU ↗`)**:
  - Кнопка `RU ↗` в тулбаре ответа ассистента надёжно находит текст шагов ассистента в DOM виртуализированного списка чата (`data-chat-flow-kind="assistant-step"`), корректно очищает от служебных элементов действий и отображает блок перевода `🌐 Перевод на русский` непосредственно под сообщением с кнопками `📋 Копировать` и `✕`.
- **Живая типографика и алиасы**:
  - Быстрая автозамена двойного дефиса на длинное тире (`--` → `—`) и кавычек-ёлочек (`""` → `«»`).
  - Разворачивание русских слэш-алиасов (`/цель `, `/план `, `/сжать `, `/справка `, `/задача `, `/контекст `, `/экспорт `).

---
## 0.2.9 — 2026-09-09

### Исправления и улучшения (Smart Russian UX Runtime, #162)
- **Живая типографика в поле ввода (Live Typography)**:
  - Исправлена синхронизация с управляемым React-компонентом composer (`<textarea>`): переход на прямой вызов дескриптора прототипа (`HTMLTextAreaElement.prototype.value.set`) и отправка синтетических событий `input` и `change` с отслеживанием `_valueTracker`.
  - Предотвращено затирание форматированных кавычек («»), длинного тире (—), многоточия (…) и неразрывных пробелов при перерендере React 18/19.
- **Русские алиасы слэш-команд (Slash Command Expansion)**:
  - Автоматическое разворачивание алиасов (`/цель` → `/goal`, `/план` → `/plan`, `/сжать` → `/compact`, `/помощь` → `/help`, `/очистить` → `/clear`, `/память` → `/memory` и др.) при нажатии пробела или клавиши Enter.
  - Поддержка вставки и набора команд с сохранением позиции курсора.
- **Фонетическая транслитерация (Alt+T)**:
  - Корректная работа сочетания клавиш `Alt+T` в поле ввода с двусторонней конвертацией (`lat2cyr` / `cyr2lat`).
  - Поддержка транслитерации как всего поля, так и выделенного фрагмента текста (`selectionStart` / `selectionEnd`).
- **Стили системного промпта в карточке настроек (SettingsCard)**:
  - В карточку настроек добавлен интерактивный селектор пресета стиля ответов агента (`agentPromptPreset`):
    - «Технический эксперт» (строгая терминология, чистый код);
    - «Технический писатель» (структурированный Markdown, таблицы, типографика);
    - «Лаконичный режим» (краткие ответы без лишней вводной воды).
  - Добавлены чекбоксы управления живой типографикой инпута (`liveInput`) и русскими алиасами команд (`slashAliases`).
- **Перевод реплик ассистента на русский (Turn Translate Action)**:
  - Кнопка `RU ↗` в панели действий реплик ассистента (`conversation.chat.assistant-actions`) теперь выполняет полноценный перевод содержимого реплики на русский язык с сохранением блоков кода (` ```...``` `).
  - Выводится элегантная карточка перевода с кнопкой быстрого копирования в буфер обмена (`📋 Копировать`) и кнопкой закрытия (`✕`).
- **Экспорт сессии в Markdown (Localized Markdown Export)**:
  - В шапку сессии (`conversation.session.header.utilities`) добавлена кнопка `📥 MD` рядом с переключателем `RU ⇄ EN`.
  - Экспортирует текущий диалог в чистый, красивый Markdown-файл с метаданными сессии, ролями и русскими заголовками.

---

## 0.2.8 — 2026-09-09

### Добавлено (Smart Russian UX, #158)
- Быстрый переключатель RU ⇄ EN в шапке диалога (`conversation.session.header.utilities`).
- Библиотека чистых функций типографики и транслитерации (`lib/pure.js`).
- Пресеты стилей системного промпта агента.
- Схема настроек `RussianLangSettingsSchema` с поддержкой пресетов и флагов UX.

---

## 0.2.7 — 2026-09-08

### Новое
- **100% локализация ядра DeepSeek Harness v0.1.5-alpha.1 и обновлений экосистемы плагинов (280 ключей)**:
  - **Ядро DSH v0.1.5-alpha.1** (+89 ключей):
    - `sidebarRight`: разделитель панелей, сворачивание/разворачивание, управление окнами и вкладками.
    - `sidebarFiles`: встроенный файловый проводник рабочей области, фильтрация, контекстное меню файлов.
    - `sidebarTextpreview`: предпросмотр текстовых файлов, пагинация больших файлов, перенос строк и перезагрузка.
    - `open-in-app`: интеграция с внешними редакторами и системными приложениями (Explorer, Finder, Terminal, VSCode, Cursor, Sublime, Windsurf).
    - `chat` & `conversation`: статистика сессии (LLM time, TPS, TTFT, использование токенов), очередь сообщений (`queue`, `steer`), Drag & Drop вложений и изображений, обновление системного промпта.
    - `command` & `goal`: расширенное управление целями диалога, новые команды `/compact`, `/export`, `/feedback`, `/goal`, `/permission`, `/plan`.
  - **Плагины экосистемы DSH** (+191 ключ):
    - `dsh-clinebot` (67 ключей): полная русификация провайдера ClineBot / ClinePass, авторизация, управление квотами 5h/weekly, каталог подписочных моделей, телеметрия и авторегистрация.
    - `betterSidebar` (32 ключа): управление маскированием конфиденциальных данных ([REDACTED]), удаление/переименование файлов и папок, интеграция с 18 плагинами вкладок.
    - `dsh-kanban` (18 ключей): автосинхронизация сессий чата с карточками, шаблоны задач (Bugfix, Feature, Refactor, Release, Security), уровни глубины рассуждений модели (Reasoning Effort).
    - `settings.pluginConsole` (11 ключей): слияние индексов витрины плагинов, поддержка интранет/локальных зеркал и пользовательских git-источников (Gitee, GitLab, Gitea).
    - `dsh-skill-explorer` & `settings.commandcode`: изоляция навыков рабочих областей, очистка устаревших моделей каталога.
- **Метрики релиза v0.2.7**:
  - Полная поддержка **DeepSeek Harness v0.1.5-alpha.1+**.
  - Ключей в интерфейсе ядра: **1144** (100.0% покрытие).
  - Ключей в плагинах экосистемы: **4251** (100.0% покрытие).
  - Суммарно ключей UI: **5395** (100.0% покрытие, 0 машинных черновиков, 0 непереведённых, 0 плохих плейсхолдеров).
- **Тесты**: Добавлен модульный тест `test/test_v027_core_and_plugins.mjs` (общее число проверок в наборе — 85).

---

## 0.2.6 — 2026-09-07

### Новое
- **100% локализация новых модулей и обновлений интерфейса (62 ключа)**:
  - `dsh-kanban` (+27 ключей): блок приёмки и ревью задач (Acceptance Gate), чек-лист критериев готовности Definition of Done (DoD), комментарии и обсуждения агента/пользователя, отчёты о выполнении.
  - `dsh-session-control` (+27 ключей): массовые операции с сессиями (bulk pin/hide/unhide/label), система пользовательских меток, быстрый переход `Alt+K`, группировка по периодам и экспорт сессий в Markdown.
  - `dsh-context` (+6 ключей): визуализация ленты ДНК контекста в порядке чтения моделью (`browser.dna`, `browser.dnaTip`), статусы загрузки истории и фильтрация графиков тренда.
  - `@vlln/dsh-navbar` (+2 ключа): быстрое закрепление и открепление реплик ассистента в ленте сообщений (`action.pin`, `action.unpin`).
- **Эффективное покрытие рантайма**: 100.0% (4713 / 4713 ключей), 0 машинных черновиков, 0 ошибок плейсхолдеров, полное соответствие глоссарию.
- **Тесты**: Добавлен модульный тест `test/test_v026_plugins.mjs` (общее число проверок в наборе — 82).

---

## 0.2.5 — 2026-09-06

### Новое
- **100% локализация 13 новых и обновлённых плагинов экосистемы DSH (659 ключей)**:
  - `@noob-stupid/dsh-plugin-console` (`settings.pluginConsole`, 314 ключей) — Консоль управления плагинами, диагностика, развёртывание через ИИ, каталоги и источники репозиториев.
  - `dsh-better-input` (`better-input`, 123 ключа) — Умный голосовой ввод, распознавание речи, оптимизация промптов через LLM, конвертер файлов и шаблоны.
  - `@jhckevin/dsh-auto-review` (`settings.autoReview`, 71 ключ) — Изолированный ИИ-ревьюер опасных и сетевых действий, уровни риска, предохранители и песочница.
  - `@goodandready/dsh-session-control` (`dsh-session-control`, 49 ключей) — Управление активными сессиями, закрепление, скрытие, поиск и архивация.
  - `@goodandready-private/dsh-memory-brain` (`@goodandready-private/dsh-memory-brain`, 19 ключей) — Долгосрочная векторная память и граф знаний.
  - `@goodandready-private/dsh-llm-wiki` (`dsh-llm-wiki`, 17 ключей) — База знаний LLM Wiki, синхронизация с CouchDB и автоиндексация.
  - `@goodandready-private/dsh-task-tracker` (`dsh-task-tracker`, 13 ключей) — Интеграция с внутренним трекером задач и тикетов.
  - `@goodandready/dsh-usage-guard` (`dsh-usage-guard`, 13 ключей) — Мониторинг расхода токенов и защита от зацикливаний.
  - `@log.li/dsh-automode` (`dsh-auto-mode.permission`, 8 ключей) — Политики безопасности и разрешения автономного режима.
  - `@goodandready-private/dsh-plugin-guard` (`dsh-plugin-guard`, 8 ключей) — Страж целостности активных плагинов и профилей DSH.
  - Обновления локализации для `dsh-context`, `@goodandready/dsh-context-lens`, `@goodandready/dsh-shadow-auditor`, `dsh-vision-bridge`, `dsh-messenger-gateway`, `dsh-key-rotation`, `dsh-gitea`.
- **Эффективное покрытие рантайма**: 100.0% (4752/4752 ключей), 0 машинных черновиков, 0 ошибок плейсхолдеров, полное соответствие глоссарию терминов.
- **Тесты**: Добавлен модульный тест `test/test_v025_plugins.mjs` (общее число проверок в наборе — 78).

---

## 0.2.4 — 2026-09-05

- feat(i18n): 100% ручное покрытие всех обновлений ядра DSH и 13 новых плагинов (суммарно +871 новый ключ, 0 машинных черновиков).
- feat(core): добавлены и обновлены официальные словари для ядра DSH (+441 ключ):
  * `trajectory` (160 ключей): детальная траектория рассуждений модели, инспекция шагов, тайминги, токены, ход мыслей, вызовы инструментов и метрики производительности (PR #151, issue #150).
  * `schedule.catalog` (18 ключей): каталог и планировщик периодических задач (интервалы every/once, относительные сроки, статусы overdue/scheduled) (PR #151).
  * `settings.pluginInventory` (20 ключей): инвентарь плагинов, фильтры пресетов, статусные теги и глобальные настройки (PR #151).
  * `settings.plugins` (15 ключей): настройки выбора модели субагентов, разрешение конфликтов, наследование модели (PR #151).
  * `settings.models` (7 ключей): кастомные base URL, поиск моделей, мастер настройки (PR #151).
  * `settings.theme` (5 ключей): управление размером шрифта диалога (PR #151).
  * `reference` (7 ключей), `settings` (6 ключей), `permission.access` / `settings.permission` (6 ключей), `slash.menu` (4 ключа), `settings.agentPreset` (3 ключа), `subagent` (3 ключа), `plan` (2 ключа), `skill` (2 ключа), `session-log-download` (1 ключ), `workspace` (1 ключ).
- feat(plugins): добавлены и обновлены словари для 13 плагинов экосистемы DSH (+430 ключей):
  * `@goodandready/dsh-vision-bridge` (81 ключ): шлюз компьютерного зрения, каналы провайдеров, блюр лиц на скриншотах, бенчмарки и автопоиск моделей Ollama (PR #151).
  * `@goodandready/dsh-messenger-gateway` (71 ключ): Telegram-шлюз, обмен текстом, голосом, фото и документами, превью диаграмм/таблиц, polling (PR #151).
  * `@goodandready/dsh-key-rotation` (67 ключей): ротация API-ключей, пулы, бюджеты, автоматический failover при исчерпании квот (PR #151).
  * `dsh-kanban` (67 новых ключей): экспорт/импорт доски в JSON, метрики задач, фильтры исполнителей и просроченных карточек (PR #151).
  * `dsh-gitea` (42 ключа): интеграция Gitea/Forgejo, PR, CI/CD статусы, репозитории (PR #151).
  * `@goodandready/dsh-cost-meter` (27 ключей): счётчик стоимости, валюты, курсы пересчёта, пиковые/ночные тарифы (PR #151).
  * `@goodandready/dsh-time-machine` (20 ключей): машина времени, автоснапшоты, diff и откат состояния рабочей области (PR #151).
  * `@goodandready/dsh-context-lens` (15 ключей): инспектор контекста, сжатие логов тестов Jest/Pytest (PR #151).
  * `settings.commandcode` (13 новых ключей): правила маршрутизации конкретных моделей на назначенные аккаунты (PR #151).
  * `@goodandready/dsh-shadow-auditor` (12 ключей): аудит безопасности, блокировка опасных команд, сканирование секретов в diff (PR #151).
  * `dsh-market` (9 новых ключей): защита от несовместимости версий DSH и экран восстановления при сбоях (PR #151).
  * `dsh-clinebot` (4 ключа): настройки интеграции ClineBot (PR #151).
  * `dsh-lanmode` (2 ключа): локальный режим сети (PR #151).
- test: добавлен тестовый набор `test/test_v024_plugins.mjs` (общее число тестов выросло до 75, все пройдены) (PR #151).

## 0.2.3 — 2026-09-05

- fix(settings): исправлен путь записи и чтения настройки «Русский промпт агента» (`agentPrompt`). UI теперь записывает флаг напрямую в верхний уровень схемы `agentPrompt` (`scope.set('agentPrompt', ...)`), благодаря чему сервер корректно активирует секцию системного промпта `dsh-russian-lang-agent-prompt` (Fixes #148, PR #149).
- test: добавлен юнит-тест `test/test_agent_prompt_setting.mjs` для проверки чтения/записи `agentPrompt` и серверной схемы (PR #149).

## 0.2.2 — 2026-09-04

- feat(i18n): 100% ручное покрытие всех установленных плагинов DSH (4752 ключа UI, 0 машинных черновиков).
- feat(plugins): добавлены официальные словари для ключевых модулей экосистемы DSH (+1149 ключей):
  * `dsh-config-manager` (5 пространств имён, 755 ключей):
    - `config-manager` (304 ключа): экспорт, импорт, выборочные бэкапы, шифрование паролем архивов, планировщик локальных бэкапов (PR #147, issue #146).
    - `config-manager-sync` (173 ключа): удалённая синхронизация через приватные Git-репозитории и WebDAV, автосинк, трёхстороннее слияние и разрешение конфликтов (PR #147).
    - `config-manager-market` (131 ключ): каталог конфигурационных пресетов сообщества, валидация безопасности и предварительный просмотр перед импортом (PR #147).
    - `config-manager-recovery` (96 ключей): аварийное восстановление повреждённых профилей, снапшоты состояния и откат (PR #147).
    - `config-manager-history` (51 ключ): журнал аудита миграционных и деструктивных операций (PR #147).
  * `dshhub-market` (namespace `dshhub-market`, 394 ключа): официальный маркетплейс dshhub.co, поиск, фильтры, управление плагинами, пресеты и резервные копии (PR #147).
- feat(dom): добавлен DOM-переводчик для карточки настроек `@liustack/modsearch` (PR #147).
- test: добавлен тестовый набор `test/test_v022_plugins.mjs` (общее число тестов выросло до 68) (PR #147).

## 0.2.1 — 2026-09-04

- feat(i18n): 100% ручное покрытие всех установленных плагинов DSH (3603 ключа UI, 0 машинных черновиков).
- feat(plugins): добавлены официальные словари для новых плагинов DSH:
  * `dsh-plugin-subagent-director` (namespace `settings.subagentDirector` / `subagent-director`, 68 ключей): центр управления субагентами Subagent Director, шаблоны ролей, привязка персон и ограничение наборов инструментов (PR #145, issue #144).
  * `@goodandready/dsh-moa` (namespace `dsh-moa`, 38 ключей): интерфейс Mixture of Agents (MoA), настройка пресетов `/moa`, модели-судьи и параллельные советники (PR #145).
  * `dsh-personal-directive` (namespace `personalDirective`, 5 ключей): переключатель персональных системных директив в шапке чата (PR #145).
- feat(update): синхронизированы словари с новыми версиями апстрим-плагинов (+195 ключей):
  * `dshmarket` v1.42.0 (+95 ключей): настройки GitHub Proxy/ускорения, кастомные зеркала, статус загрузки, fallback-маршруты.
  * `dsh-better-sidebar` v0.18.0 (+54 ключа): трекинг файловых операций в сессии (чтение, запись, правка, сплиты diff-просмотра).
  * `dsh-context` v0.41.3 (+27 ключей): статистика времени активности и задержек (TTFT, LLM Gen, Active Time).
  * `dsh-skill-hub` v0.3.7 (+30 ключей): переключение отслеживаемых версий репозиториев (Releases, Branches, пользовательские теги).
  * `@michengai/dsh-skills-manager` v0.1.38 (+19 ключей): сводки активных навыков и ссылки на репозиторий/issues.
  * `@mars-sea/dsh-commandcode-provider` v0.10.0 (+30 ключей): статусные бэйджи аккаунтов (активен, кулдаун, невалидный ключ).
- feat(dom): добавлен DOM-перехват и перевод интерфейса `dsh-hooks` (управление вебхуками, события, сохранение) (PR #145).
- test: добавлен тестовый набор `test/test_v021_plugins.mjs` (общее число тестов выросло до 66) (PR #145).

## 0.2.0 — 2026-09-03

- feat(i18n): 100% ручное покрытие всех установленных плагинов DSH (3044 ключа, 0 машинных черновиков).
- feat(plugins): добавлены официальные словари для ключевых UI-модулей:
  * `dsh-opencode-palette` (namespace `opencode-palette`, 27 ключей): выбор тем, шрифтов, стилей и палитр (PR #143, issue #142).
  * `dsh-univer-office` (namespace `univer`, 53 ключа): интерфейс Univer Office, статусы Gateway, слияние и ревью документов (PR #143).
  * `@linxin666/dsh-client-ui-skill-explorer` (namespace `dsh-skill-explorer`, 53 ключа): Центр навыков Skill Center, создание и управление скиллами (PR #143).
- feat(dom): встроен интеллектуальный DOM-переводчик для плагинов без стандартного i18n:
  * `dsh-auto-collapse`: автоматический перевод китайского интерфейса (сворачивание процессов, мысли, статусы) через карту `ZH_RU` (PR #143).
  * `@dsh-external/dsh-visualize`: русификация всплывающих подсказок потокового предпросмотра (`[title]`) (PR #143).
  * `dsh-effort-slider`: локализация градаций уровней рассуждений модели (`Low`, `Medium`, `High`, `Effort`) (PR #143).
- test: добавлен тестовый набор `test/test_hardcoded_plugins.mjs` (общее число тестов выросло до 63) (PR #143).

## 0.1.33 — 2026-09-03

- fix(runtime): устранена утечка памяти keydown-обработчика (добавлен `removeEventListener` в `ctx.effect`, PR #141, issue #140).
- feat(layout): Alt+L теперь распознаёт как латинскую 'L', так и русскую клавишу 'Д' (`ev.code === 'KeyL' || ev.key === 'д'`), обеспечивая безотказную работу при активной кириллической раскладке (PR #141).
- fix(pure): `fill()` защищён от `TypeError` при отсутствии `params`; `formatNumber`, `formatCurrency`, `formatRelativeTime` возвращают пустую строку для `null` и пустых значений вместо ложных `0` и `57 лет назад` (PR #141).
- test: `npm test` расширен до полного набора проверок (18 тестовых файлов); добавлены unit-тесты граничных условий и раскладок (PR #141).
- i18n: вычитано и переведено в ручные словари 1052 ключа (`agentTeams`, `betterSidebar`, `dsh-market`, `context-doctor`, `settings.ollama-cloud`, `settings.commandcode`, `usageStats`, `dsh-context`). Исправлены смысловые ошибки машинного перевода (например, `Left team` -> «Покинул команду», `Cursor` IDE, `Settled` -> «Завершено»). Доля ручного выверенного перевода достигла **100.0%** (2899 ключей), черновики MT сведены к 0 (PR #141).

## 0.1.32 — 2026-09-03

Hotfix-релиз: восстановление работоспособности типографики «ё» и починка
контура качества, который эту поломку пропустил.

- fix(typography): восстановление «ё» не работало и содержало правило, портящее текст (issue #132). Два дефекта гасили друг друга. Первый: `YO_BLACKLIST` сверялся со словом, содержащим «ё», тогда как в списке лежало написание через «е» — условие не срабатывало никогда, и пара `все -> всё` уезжала в бандл вопреки списку, комментарию в коде, описанию в карточке настроек и юнит-тесту. Второй: пары якорились через `\b`, который в JavaScript определён по `\w` = `[A-Za-z0-9_]` и границы перед кириллицей не даёт — из 166 пар срабатывали ровно три (`еще`, `Еще`, `ЕЩЕ`), остальные были мёртвым кодом. Починка только якорей немедленно включила бы порчу текста, поэтому порядок исправления обратный: сначала блеклист. Блеклист расширен омографами (`все`, `всем`, `чем`, `нем`, `моем`, `берет`, `черт`, `черта`, `черту`, `чертов`) и мусором корпуса (`ето`, `пеп`, `хен`); `берет` убран и из ручного списка. Якорь заменён на lookaround по кириллице, регистр восстанавливается из совпадения — пара нужна одна на слово вместо трёх на регистр. В бандле 137 живых пар вместо 166 мёртвых.
- refactor(lib): чистая половина вынесена в `lib/pure.js` (issue #133). 1069 строк JavaScript жили внутри питоновского литерала в `build.py`: импортировать оттуда было нечего, и тесты держали собственные копии функций, расходившиеся с оригиналом молча. Теперь типографика, плюрализация, `fill`, склонения, поисковая морфология, humanizer, `makeIssueUrl` и детектор раскладки лежат в отдельном ES-модуле; `build.py` читает его, снимает `export` и вставляет в бандл, тесты импортируют напрямую. Один исходник на бандл и на тесты.
- test(qa): 12 тестов переведены с копий на импорт из `lib/pure.js` (issue #133). Мутационная проверка: шесть намеренных поломок (кавычки, пунктуация, nbsp, возврат сломанного `\b`, балл нечёткого поиска, порог распознавания раскладки, относительное время) роняют соответствующий тест — раньше не роняла ни одна. Из `test_card_yo.mjs` убрано тождественно истинное утверждение `!pairs.some(([e, y]) => e === 'все' && y === 'все')`, которое заявляло защиту от `все -> всё` и не проверяло ничего, и питоновское `%`-форматирование, дававшее `NaN` в тексте ошибки. Добавлены проверки на омографы, на живую границу слова, на регистр и на идемпотентность.
- ci: прогоняются все тесты и сверяется бандл (issue #134). CI гонял 5 файлов из 14 — фичи 0.1.29–0.1.31 уехали в релиз непроверенными. Теперь `node --test test/*.mjs`, python-тесты `test_extract`/`test_overflow`, линтер словарей и новый шаг `git diff --exit-code lib/client.js`: закоммиченный бандл обязан совпадать со сборкой, иначе `npm publish` уедет кодом, который не проходил ревью. Сборка перенесена в начало — тесты читают свежий артефакт.
- fix(build): бандл писался в текстовом режиме и на Windows получал CRLF (issue #137). Без `newline=` Python подставляет `os.linesep`, поэтому один исходник давал разные байты: 1096 CR под Windows против 0 под Linux. Дефект сделал бы сверку бандла из #134 нестабильной по платформе релизящего. Перевод строки зафиксирован явно здесь и в `tools/mt_autofill.py`.
- fix(i18n): MT-реестр получил статус вычитки, счётчики покрытия перестали врать (issue #135). В записях `mt-registry.json` не было поля `status`, вычитанная строка была неотличима от неглядя сгенерированной, и очередь ревью на 2173 строки не могла уменьшаться — прогресс некуда записать. Добавлено `status: draft | reviewed` (2170 записей размечены), `mt_fallback.py` не сбрасывает `reviewed`, очередь генерируется только из `draft`. `check-coverage.py` печатал «переведено: 2899 (ручных 2320 + MT 1802)» — слагаемые считались по своим источникам и пересекались; теперь раскладка непересекающаяся и сходится к итогу: ручных 1847, MT выверенных 0, MT без вычитки 1052, с предупреждением, что 36.3% интерфейса — черновой машинный перевод.
- fix(tools): `mt_autofill.py` требовал API-ключ раньше ветки первого запуска (issue #136). Базовый срез `upstream/plugins-snapshot.json` — производное локальное состояние (байт-в-байт дубль `plugins-en.json`), он уходит в `.gitignore` по согласованию с владельцем. В свежем клоне среза нет, а его создание перевода не требует — проверка ключа перенесена ниже, первый запуск теперь отрабатывает без `OPENROUTER_API_KEY`.
- fix(test): `test_extract.py` падал на Windows при уборке временного каталога. Причина не в уборке: тест делал `os.chdir` внутрь `TemporaryDirectory`, а Windows не удаляет каталог, который является текущим для процесса (`WinError 32`). CWD возвращается в `finally` до выхода из контекста.
- docs: README документировал несуществующие ключи конфигурации (issue #138). Пример `settings.yaml` обещал пространство `dsh-russian-lang` с подключами `typography.quotes/dashes/nbsp`, секцией `layoutFix` и `spellcheck` — ничего этого в схеме нет, а реальные `yo` и `agentPrompt` не были описаны. Блок приведён к схеме из `lib/index.js`. Заявка «100% покрытие ядра» дополнена честной оговоркой о доле машинного перевода со ссылкой на очередь вычитки.
- fix(ui): ссылка «сообщить о проблеме перевода» подставляла версию `0.1.29` в пакете 0.1.31 — строка была вписана руками. Версия берётся из `package.json` на сборке.

## 0.1.31 — 2026-09-02

- feat(search): Russian search morphology and fuzzy matching for command palette (PR #128, issue #124). Adds `stemRussian` Porter-like stemmer and `fuzzyMatchRu` with support for inflected word forms, exact substrings, and layout transliteration in command palette search (`Ctrl+K`) and slash menu (`/`).
- feat(errors): human-readable system and network error translator (PR #129, issue #125). Adds `humanizeError` translating Node.js POSIX (`ENOENT`, `EACCES`, `ECONNREFUSED`), HTTP (`401`, `403`, `404`, `429`, `500`), and API rate limit errors into friendly Russian explanations with actionable troubleshooting hints.
- feat(tools): upstream watchdog and delta detector for DSH core updates (PR #130, issue #126). Adds `tools/upstream_watch.py` for automatically detecting newly added upstream strings and generating translation review queues (`upstream/review-queue.md`).
- feat(tools): comprehensive dictionary linter and git pre-commit hook (PR #131, issue #127). Adds `tools/lint_translations.py` for static JSON validation, placeholder verification, and glossary compliance with `--install-hook` support.

## 0.1.30 — 2026-09-02

- feat(ui): report translation issue and request plugin translation via GitHub Issues (PR #120, issue #112). Adds `makeIssueUrl` helper and UI link in SettingsCard generating pre-filled GitHub issues for `GooDAnDReaDY/dsh-russian-lang`.
- feat(ui): plugin localization coverage inspector and badge status (PR #121, issue #107). Adds `getPluginLocalizationStatus(ns)` helper for checking dictionary coverage of installed plugins.
- feat(tools): export and import utilities for PO, XLIFF 1.2, and CSV formats (PR #122, issue #109). Adds `tools/export_i18n.py` and `tools/import_i18n.py` with placeholder validation.
- refactor(bundle): optimize dictionary payload serialization and client size (PR #123, issue #110). Compact JSON serialization reducing client bundle footprint.

## 0.1.29 — 2026-09-02

- feat(tools): formalize DSH glossary (`glossary.json`) and validate in CI (`tools/term_check.py --ci`) (PR #115, issue #108). Prevents calques and terminology divergence across 50+ namespaces.
- feat(i18n): format helpers for numbers, currencies, and relative time (PR #116, issue #106). Adds `formatNumber` (non-breaking thousand separator), `formatRelativeTime` (`Intl.RelativeTimeFormat`), `formatCurrency` in `ru-RU`, and extends `fill()` with format specifiers `{val:number}`, `{val:reltime}`, `{val:currency}`.
- feat(i18n): smart inflection engine for entity names and roles (PR #117, issue #104). Adds `inflect(phrase, case)` supporting `gen`, `dat`, `acc`, `ins`, `pre` Russian cases with safe fallback for Latin and acronyms.
- feat(typography): streaming typography and Chinese quote normalization (PR #118, issue #105). Normalizes Chinese quotes (`“”`, `『』`) into Russian guillemets (`«...»`), cleans up stray spaces before punctuation, and isolates LaTeX math ($...$) and code blocks.
- test(qa): UI text overflow and truncation detector (PR #119, issue #111). Adds `test/overflow.py` and `test/test_overflow.py` for automated inspection of UI elements (`scrollWidth > clientWidth`).

## 0.1.28 — 2026-09-02

- fix(settings): карточка настроек работает на DSH v0.1.2-alpha.2 (PR #100, #101, #113; issues #99, #97). Три слоя проблем: (1) `scope.set` в ядре теперь возвращает `Promise<void>` — ошибка приходит через promise rejection, старый `try/catch` её глотал; добавлены `.catch(...)` для promise-канала и внешний `try/catch` для sync. (2) Устаревший monkey-patch `runtime.host.getSnapshot/set` конфликтовал с новой settings-mirror и ломал запись namespace — удалён полностью. (3) Контракт slot-inject изменился: renderer вызывает `entry.inject()` и разворачивает результат прямо в props (`props.scope`/`props.runtime`/`props.toggleRu`), а не отдаёт `props.inject` — карточка читала устаревший формат и получала `scope === undefined`, из-за чего каждый клик падал «Cannot read properties of undefined (reading 'set')». Читаем новые props с фолбэком на старый контракт.
- feat(locale): русский для dsh-skill-hub и @michengai/dsh-skills-manager (PR #103; issue #102). Оба плагина регистрируют только `{zh, en}` — добавлены `ru-plugins/11-skill-hub.json` (~170 ключей) и `ru-plugins/12-skills-manager.json` (~170 ключей), перевод по их английским словарям, плейсхолдеры сохранены. Бандл: 51 namespace, 3700 ключей.
- feat(locale): DOM-перевод панелей в обход locale-ядра (PR #113; issue #102). Панель dsh-skill-hub выбирает словарь по `documentElement.lang` и понимает только en/zh — русский в неё не попадает никак. Добавлен третий DOM-проход (рядом со spellcheck/типографикой): при активном русском CJK-текстовые узлы и атрибуты title/placeholder/aria-label заменяются по карте ZH_RU (348 пар), собранной на сборке из zh-референсов плагинов (`zh-refs/*.json`) и нашего перевода тех же ключей; шаблонные пары («共 {count} 个技能») — через регексы, значения переносятся в ru-шаблон («Всего навыков: 56»). MutationObserver удерживает переведёнными перерендеры React; при уходе с русского наблюдатель отключается.

## 0.1.26 — 2026-08-31

- fix(client): pass lookup chain to core lookup for DSH 0.1.2 (PR #94, issue #93). The translate wrapper called `this.lookup(ns, key)` with two args; DSH 0.1.2 `lookup(ns, key, chain)` requires a third argument (the locale chain) and iterates it, so `chain === undefined` threw `chain is not iterable`. That crashed `timeLabel` → `SessionNodeItem` → the whole `sidebar.workspaces` slot, so the conversation list vanished with no mention of Russian. Now the wrapper asks the method's arity (`lookup.length`) and passes the chain only when the core expects it, taking it from the core's `fallbackChain` (else `[active]`). Old 2-arg cores keep working. Adds `test/test_lookup_chain.mjs`.

## 0.1.24 — 2026-08-30

- fix: 0.1.23 вышла БЕЗ обещанного словаря `dsh-kanban`. Локальный `main` и `main` на GitHub разошлись: правка канбана лежала только на GitHub, релизные коммиты 0.1.21–0.1.23 — только локально, и сборка 0.1.23 шла по локальной ветке (49 пространств, 3309 ключей). 0.1.24 собрана после слияния обеих линий: 50 пространств, 3530 ключей, `dsh-kanban` внутри.

## 0.1.23 — 2026-08-30

- feat: русский словарь для `dsh-kanban` (221 ключ, `ru-plugins/10-kanban.json`). Плагин доски перестал нести свой `ru`: встроенные локали ядра — английская и китайская, русский даёт этот пакет. Пространство убрано из `self-ru.json` — пока оно там стояло, сборка пропускала его как «плагин локализовался сам». Выкатывать раньше dsh-kanban 0.1.12.

## 0.1.22 — 2026-08-30

- fix(client): use real LocaleRuntime API (addLanguage/setLocale) for RU switch (PR #88). 0.1.21 was published from a stale local main and missed this fix; 0.1.22 is the corrected build.

## 0.1.21 — 2026-08-30

- fix(client): use real LocaleRuntime API (addLanguage/setLocale) for RU switch (PR #88). Old code wrote runtime.snapshot / called runtime.publish() — neither exists on DSH 0.1.2-alpha, so switching silently no-op'd. Now the native Language menu lists Русский and selecting it switches the UI.

## 0.1.20 — 2026-08-30

- fix(client): register settings card via slots.inject so the slot is declared (PR #87). Direct register threw "slot is not declared" on DSH 0.1.2-alpha and the card never rendered; 0.1.19 only guarded the error. Now matches dsh-context/dsh-key-rotation.

## 0.1.19 — 2026-08-30

- fix: self-ru(dsh-key-limits) — private plugin self-localizes, exclude from bundle (49 ns/3309), fixes prod Failed to load (#81 suppl.)
- fix(client): guard settings.plugin.item slot (hotfix #85) already in 0.1.18+main, keep in release

# Changelog

## 0.1.18 (2026-08-29) — hotfix

- `fix(client)`: drop the `@deepseek-ai/dsh-client-ui-primitives` require and
  use a pure SVG chevron. That module is not registered in the module table of
  the current DSH core, so requiring it aborted our plugin's loader entry and
  broke production. Our bundle now requires only `react`. (#81)

## 0.1.16 (2026-08-27)

- `feat(client)`: RU/EN layout indicator near the chat input; click converts
  the current text (Alt+L behaviour). (#66)
- `feat(client)`: layout fixer learns from accepted corrections — a
  per-session local dictionary reduces false positives. (#67)
- `feat(host)`: optional Russian agent system prompt
  (`russian-lang.agentPrompt`), exposed in the settings card. (#68)
- `feat(tools)`: auto-fill new plugin keys into the MT queue
  (`tools/mt_autofill.py`, daily cron). (#69)
- `fix(client)`: settings card uses the core chevron icon (with fallback).
- `fix(mt)`: corrected 39 placeholder mismatches in shipped translations.
- `ci`: build npm `.tgz` artifact for the isolated test server.

## 0.1.15 (2026-08-26)

- `fix(client)`: settings-card checkboxes update instantly. Typography and ё
  toggles were controlled by async host state, so React reverted every click;
  now they use optimistic local state and sync from the host afterwards. The
  Russian switch reacts to locale changes.
- `feat(client)`: descriptive captions for «Типографика вывода» and
  «Буква ё» in the settings card.

## 0.1.13 (2026-08-26)

- `feat(dict)`: translate dsh-spend (namespace `usageStats`, 129 keys) — now
  shipped in the bundle; self-ru scanner no longer misfires on `{zh,en}`-only
  registrations. (#55)
- `feat(build/CI)`: test/_repro.cjs exits non-zero on a broken bundle; MT
  registry placeholder gate + honest runtime coverage metric (100%). (#55)
- `fix(ci)`: offline runner — dropped setup-python, added term_check warning
  step. (#55)
- `fix(tools)`: upstream_check no longer pushes directly to main (commits to
  `upstream/snapshot` for a PR instead). (#55)
- `feat(client)`: settings card now shows the Alt+L layout-convert hint. (#55)
- `chore(ops)`: weekly `freq_refresh` cron added.

## 0.1.10 (2026-08-25)

- `feat(client)`: settings card. New «Русская локализация» card in
  Настройки → Плагины → Настройки плагинов: switch Russian on/off (goes
  through the runtime, no syncFlag fight), typography toggle, ё toggle,
  overrides count. Collapsed by default; form gated on the snapshot status;
  rl- prefixed styles on theme variables.
- `feat(build)`: corpus-derived ё dictionary. build.py generates е→ё pairs
  from the bundled frequency corpus (144 pairs + 22 curated); ambiguous
  «все» is blacklisted. typography.yo stays default-off.
- `feat(tools): term_check.py` — en→ru terminology consistency report across
  all namespaces (27 contextual divergences documented).
- `chore(dict)`: plugin dictionaries refreshed from the production profile
  (plugins-en.json 16 ns / 1815 keys); MT filled 102 new dsh-market keys,
  25 bad drafts rejected by the new placeholder validation.

## 0.1.9 (2026-08-25)

- `feat(tools)`: placeholder validation in MT apply. `mt_fallback.py --apply`
  retries translations whose `{placeholders}` diverge from the en original and
  drops persistent mismatches instead of shipping raw `{...}` into the UI.
  Provider is injectable for tests. (#45)
- `feat(tools): self-ru namespace autodetection`. New
  `tools/self_ru_scan.py <profile>` writes `self-ru.json`; build.py prefers it
  over the hardcoded list, so new self-localizing plugins stop crashing the
  loader. (#45)
- `feat(tools): freq_refresh`. `tools/freq_refresh.py` downloads a fresh
  Russian frequency list and re-merges DSH domain words in one command. (#45)
- `test: full coverage for the tooling`. mt_fallback (PH rejection, retry,
  incremental registry), merge_freq (word extraction, stop-list),
  self_ru_scan (quote styles), freq_refresh (filter), upstream_check
  (diff/report). (#45)
- `ci: Gitea Actions workflow` — node tests, tool tests, build+syntax,
  coverage gate and a de-identification grep on every PR. (#45)
- `fix(tools)`: upstream_check/systemd installer no longer hardcode machine
  paths — configuration moved to environment variables.

## 0.1.8 (2026-08-25)

- `fix(plugin)`: complete the self-ru exclusion list. 0.1.7 missed
  `dsh-gitea`, `dsh-key-rotation` and `dsh-vision-bridge`, which also
  register their own ru locale; the loader still failed with «locale
  namespace X already has locale ru». All 14 self-ru namespaces are now
  excluded at build time; bundle 41→38 namespaces / 1878→1761 strings.

## 0.1.7 (2026-08-25)

- `fix(plugin)`: skip namespaces that plugins already localize to ru. 0.1.6
  added ru for namespaces the plugins themselves register as ru
  (dsh-messenger-gateway, dsh-spendmeter, task-board, settings.commandcode,
  pin, dsh-context, context-doctor, settings.ollama-cloud, plugin-store,
  usageStats, usageDashboard), which made the loader fail with
  «locale namespace X already has locale ru». Excluded at build time; bundle
  shrinks 52→41 namespaces / 2795→1878 strings.

## 0.1.6 (2026-08-25)

- `feat(tools): translate top plugins via MT`. `tools/mt_fallback.py` fills
  untranslated plugin keys through OpenRouter (deepseek/deepseek-chat) into
  `mt-registry.json`, now written incrementally so long runs survive
  interruption. 1086 keys across 16 plugin namespaces added; bundle grows
  39→52 namespaces / 1709→2795 strings. Full manual-review queue in
  `upstream/review-queue.md`; a manual translation in `ru-plugins/*.json`
  always wins over the machine one. (#3)
- `feat(tools): domain dictionary for wrong-layout detector`. New
  `tools/merge_freq.py` merges DSH vocabulary from the plugin's own
  translations into the layout-fix frequency list (8000→9181 words), so the
  detector recognises terms like «рабочая сессия». (#6)
- `test(smoke): probe feature surfaces`. Smoke reports runtime presence of
  the language/spellcheck/typography surfaces as probes. (#5)
- `fix(plugin)`: two placeholder bugs in context-doctor (`cd.updated` lost
  `{when}`, `cd.suggestions` had a stray `{n}`); surfaced once the plugin
  baseline `plugins-en.json` was committed.

## 0.1.5 (2026-08-25)

- `feat(tools): nightly upstream drift check`. `tools/upstream_check.py`
  installs the latest `@deepseek-ai/dsh`, harvests the en locale surface,
  diffs against a committed snapshot and opens/updates a `chore(upstream):`
  Gitea issue on drift. Installed as a daily cron job. (#18)
- `feat(tools): MT-fallback with review queue`. `tools/mt_fallback.py` fills
  untranslated keys via a cheap OpenRouter model into `mt-registry.json`;
  `build.py` merges those strings as ordinary keys (manual ru translation
  wins) and `--review` writes `upstream/review-queue.md`. (#22)
- `feat(tools): top-100 plugin coverage report`. `tools/top100.py` scans a
  profile's node_modules, harvests plugin locale surfaces and writes
  `docs/top.md`. (#21)
- `feat(client): wrong-layout input hint and Alt+L converter`. Detects latin
  typed instead of Cyrillic (bundled 8k Russian frequency list), shows a
  hint bar; click replaces. Cyrillic→Latin only for slash-command input.
  Alt+L converts the current input in place. (#25)

## 0.1.4 (2026-08-25)

- `feat(client): browser spellcheck on text fields`. While Russian is active,
  textareas and text/search inputs get `spellcheck=true` + `lang=ru-RU`;
  monospace fields (code, commands) are excluded, originals restored on
  language switch. (#24)
- `feat(client): plural forms for bare count keys`. The translate wrapper now
  resolves `X.one` / `X.few` / `X.many` for keys called as `t('X', {n})`
  without the core's `.one/.other` suffix; dictionaries gain correct forms for
  the context-doctor counters and workspace search hint. (#17)
- `feat(client): Russian typography pass`. Idempotent text-node post-processor:
  guillemets, em dash, non-breaking spaces after short function words;
  optional ё restoration (`russian-lang.typography.yo`, default off). Code,
  links and inputs are never touched. (#19)
- `chore(check)`: `X.few` / `X.many` are treated as intentional when the bare
  base key exists in en. (#17)

## 0.1.3 (2026-08-25)

- `feat(client): Russian plural forms (few/many)`. Wraps `runtime.translate` to
  resolve Russian plural forms (one/few/many) for keys ending in `.one`/`.other`,
  and adds `few`/`many` keys for all 7 plural pairs in the core.
- `feat(client): user-defined translation overrides`. New `russian-lang.overrides`
  settings namespace (`{ key: wording }`) applied as the top layer in translate.
- `feat(check): flag lost placeholders`. `check-coverage.py` fails on ru
  translations that drop or add `{placeholders}`; `X.few`/`X.many` are treated as
  intentional Russian plural forms.
- `feat(client): use ru-RU as the document locale`. `<html lang>` is now `ru-RU`
  (BCP 47); `Intl.PluralRules('ru-RU')`.

## 0.1.2 (2026-08-23)

- `fix(client): register html-lang subscriber synchronously`. The previous
  `ctx.effect(() => runtime.subscribe(syncLang))` deferred subscriber registration
  until after the first `publish()` from the boot path, so `<html lang>` never
  picked up the ru value on a fresh boot. Registered inline; the disposer is
  still attached through a `ctx.effect` so it tears down with the plugin.

## 0.1.1

- Initial public release. 30 DSH core namespaces (722 keys, 100% coverage) plus 9
  plugin namespaces (962 keys). Adds `Русский` as the third option in the native
  Settings - General - Language menu on unpatched cores. Persisted through the
  plugin-owned `russian-lang` settings namespace.
